# Canvas Studio 后续开发路线图

> 更新日期：2026-08-24  
> 产品边界：单用户、本地优先、不做多租户、不做权限系统、不做强制教程、不在画布内做视频剪辑合成。  
> 节点规范以 [NODE_CONTRACT_SPEC.md](./NODE_CONTRACT_SPEC.md) 为唯一依据。

## 1. 总体判断

当前最值得做的不是继续增加大量节点，而是先把已经建立的输入输出契约变成完整的扩展平台。推荐顺序是：

```text
稳定现有节点
→ 拆分节点执行器
→ 建立自动化测试门禁
→ 增加结构化 AI 节点和工作流模板
→ 增加列表/批处理编排
→ 完善媒体生产链路
→ 优化大画布性能与离线能力
```

如果跳过执行器解耦和测试，直接增加节点，`executor.ts`、节点 UI 和连线规则会再次互相耦合，后续每加一个节点都可能破坏已有节点。

## 2. 当前基线

已经具备：

- Electron 本地桌面应用、项目管理、自动保存和媒体资产落盘。
- tldraw 无限画布、自定义节点、真实连线、分组、撤销重做和右键菜单。
- 文本、图片资产、生图、视频、音频、对话、处理、JSON、代码、分镜板节点。
- 节点契约版本、稳定端口 ID、必填性、单值/多值、JSON Schema。
- 按 `nodeId + portId` 传递的运行数据包和运行前后契约校验。
- OpenAI 兼容文本/图片模型网关以及任务式视频网关。
- Markdown 对话渲染、节点 I/O 说明和上下游映射。

仍然存在的架构缺口：

- 各节点执行逻辑仍集中在全局 `executor.ts` 的 switch 中。
- 契约、连线和执行器缺少自动化行为测试。
- 只有 `json.any@1` 和 `storyboard.shots@1` 两个 Schema。
- 工作流中的“AI 转换”仍借用交互式对话节点，职责不够清晰。
- 分镜到批量生图/视频缺少受控的列表迭代语义。
- 运行结果没有完整持久化运行记录、耗时和输入摘要。

## 3. 分阶段计划

### R0：发布基线与回归保护

优先级：最高。应在继续开发新能力前完成。

任务：

1. 建立节点清单回归表，覆盖创建、编辑、拖动、复制、删除、连接、运行、保存和恢复。
2. 为旧项目快照补充契约版本迁移入口；遇到未知端口时给出明确提示，不静默猜测。
3. 检查所有手动触发按钮是否与全局运行使用相同端口和输出投影。
4. 增加本地运行错误日志，记录节点 ID、端口 ID、契约版本和错误阶段，但不记录 API Key。
5. 固定一份可回归的示例项目，至少包含文本→生图、文本→对话→JSON、JSON→分镜和处理→代码链路。

完成标准：

- 旧项目可以打开、保存并再次打开。
- 任意错误连线都能在创建时或运行时得到具体端口级错误。
- 所有现有节点通过统一回归表。
- 发布包在全新 Windows 用户目录可启动。

### R1：节点执行器解耦（契约规范 P3）

目标：新增普通节点时不再修改全局执行器。

任务：

1. 在 `NodeTypeSpec` 中增加 `executor` 或稳定的 `executorId`。
2. 定义统一接口：

   ```ts
   interface NodeExecutor {
     execute(context: NodeExecutionContext): Promise<NodeExecutionResult>
   }
   ```

3. 把文本、媒体、对话、JSON、处理、代码和分镜执行逻辑分别移动到节点目录。
4. 全局运行器只保留拓扑排序、输入收集、契约校验、取消、状态和输出登记。
5. 明确节点固定配置与运行结果的分离，禁止把上游输入反复写回节点配置导致内容累积。
6. 为长任务统一取消和超时协议。

完成标准：

- 删除 `executor.ts` 中按 `nodeType` 分发的主 switch。
- 新增一个示例节点只需增加 Spec、Body、Executor 和测试。
- 节点配置、实际输入和实际输出在数据结构上互不混用。

### R2：自动化测试与 CI 门禁（契约规范 P6）

目标：端口变化、Schema 变化和旧快照兼容问题在提交前自动暴露。

任务：

1. 引入轻量测试运行器，优先测试纯函数，不依赖 Electron UI。
2. 增加节点注册快照测试：端口 ID、类型、数量、Schema 和契约版本。
3. 增加连线矩阵测试：允许、拒绝、单值占用、重复边和环。
4. 增加运行时测试：缺失输入、错误类型、错误 Schema、缺失输出和 `any` 实际类型。
5. 增加旧快照迁移测试和工作流模板恢复测试。
6. 增加 GitHub Actions：安装、typecheck、lint、test、build。

完成标准：

- 破坏端口 ID 而不提升契约版本时测试失败。
- 错误分镜结构不能进入分镜节点。
- 主分支只能接收通过完整门禁的提交。

### R3：结构化 AI 节点与可复用模板

目标：把“交互聊天”和“工作流 AI 转换”拆开。

建议新增“AI 处理”节点：

- 输入：`in-text`、`in-json`。
- 配置：模型、系统提示词、输出模式、温度、最大输出。
- 输出模式：`text`、`markdown` 或指定 Schema 的 `json`。
- JSON 模式必须显式选择 Schema；解析失败直接报错，不把普通文本伪装成 JSON。

配套任务：

1. 保留“对话”节点用于多轮交互。
2. “AI 处理”节点用于一次性、可重复执行的工作流转换。
3. 建立“剧本→分镜”模板：文本→AI 处理→JSON→分镜板。
4. 建立角色设定、场景清单、镜头清单等版本化 Schema。
5. 模板保存节点契约版本和明确的端口 ID。

完成标准：

- 剧本结构化不依赖对话历史。
- 输出 JSON 可验证、可复跑、可单独调试。
- 脚本旧复合节点只承担历史兼容，不再创建新实例。

### R4：列表与批处理编排

目标：支持分镜列表批量生图/生视频，但不制造新的巨型复合节点。

先设计后实现：

1. 定义列表数据协议，例如 `list.items@1`，并明确元素 Schema。
2. 增加“迭代”控制节点或工作流子图机制；输入列表，逐项填充子流程变量。
3. 支持并发上限、失败策略、重试、取消和进度。
4. 每个子任务保留来源：镜头 ID、输入摘要、模型和生成资产 ID。
5. 结果输出仍为结构化列表，不把几十张图片藏在一个不可连接的节点内部。

完成标准：

- 20 个镜头可以受控地批量执行。
- 单项失败不会丢失其他成功结果。
- 中止后可以继续未完成任务。

### R5：媒体生产链路

任务：

1. 生图节点支持稳定的参考图、尺寸、种子和供应商能力差异说明。
2. 视频节点明确首帧、文本、时长和分辨率输入；不接受未声明的视频输入。
3. 增加生成资产的来源信息和重新生成入口。
4. 分镜卡片关联生成资产，但资产仍然是独立图片/视频节点。
5. 增加下载、在资源管理器中定位和复制路径。
6. 不在画布内加入视频剪辑/合成；输出交给剪映等专业工具。

完成标准：

- 任一媒体都能追溯到产生它的节点和输入。
- 粘贴、拖入和生成的图片统一成为图片节点。
- 失败任务可以重试且不会生成重复资产记录。

### R6：大画布性能与离线能力

任务：

1. 建立 100、500、1000 节点基准项目，记录首屏、拖动和保存耗时。
2. 避免拖动时遍历全部节点；连接和分组轮廓使用索引或增量更新。
3. 限制右侧面板的值预览大小，大型 JSON 和媒体只显示摘要。
4. 把 tldraw 字体、图标等外部静态资源本地化。
5. 检查视频 Range 请求和大媒体加载行为。
6. 将超大 `bodies.tsx` 按节点拆分，降低热更新和维护成本。

完成标准：

- 500 节点画布常规拖动保持流畅。
- 断网时画布、图标、字体和已有媒体可正常使用。
- 大项目保存不会冻结主要交互。

### R7：发布与数据安全

任务：

1. 为 Windows 安装包增加版本号、升级说明和数据备份提示。
2. API Key 继续只在本地使用；后续可用 Electron `safeStorage` 加密落盘。
3. 增加项目导出/导入和兼容版本检查。
4. 增加崩溃恢复、最近备份和项目修复工具。
5. 每次发布保留节点契约变更记录和迁移说明。

完成标准：

- 升级应用不会破坏旧项目。
- 用户可以导出完整项目并在另一台机器恢复。
- 仓库、日志和导出诊断包均不包含 API Key。

## 4. 推荐优先级

| 顺序 | 工作          | 原因                                 |
| ---- | ------------- | ------------------------------------ |
| 1    | R0 发布基线   | 防止继续开发时重复引入交互和保存回归 |
| 2    | R1 执行器解耦 | 决定节点系统能否真正扩展             |
| 3    | R2 测试与 CI  | 让契约规范从文档变成持续门禁         |
| 4    | R3 结构化 AI  | 直接提升剧本、分镜等核心创作能力     |
| 5    | R4 批处理     | 为多镜头生产提供正确的控制模型       |
| 6    | R5 媒体链路   | 在编排稳定后完善生成体验             |
| 7    | R6/R7         | 面向大项目和正式发布收尾             |

## 5. 明确不做

- 多租户、团队权限、审核流和账号系统。
- 强制教程、复杂的新手引导和运营后台。
- 画布内视频剪辑、时间线和成片合成。
- 没有稳定输入输出定义的临时节点。
- 为了少画几个节点而把多个步骤重新塞进一个复合节点。
