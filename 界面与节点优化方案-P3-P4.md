# 画布界面与节点优化方案（P3/P4）

## 目标

让画布优先呈现“内容与数据关系”，而不是同时呈现所有功能。节点仅表达一件事：输入、状态、产出；设置收进二级面板；真实数据依赖以锁定箭头呈现。

## 已执行的第一轮规则

| 规则 | 落地方式 | 原因 |
| --- | --- | --- |
| 默认尺寸统一 | 所有已实现节点新建时均为 `360 × 360` | 建立稳定的画布节奏，便于对齐和扫描 |
| 内容溢出不撑卡片 | 配置区与结果区在节点内滚动 | 不因对话、图片或视频结果把布局打散 |
| 一类能力一个节点 | 视频生成为独立节点；处理能力后续进入 ImageProcess / VideoProcess 的 op | 防止“一个能力一个节点”造成面板膨胀 |
| 设置与执行分离 | 常用结果/状态留在卡片，参数放进 ⚙ | 降低默认信息密度 |
| 面板按可用性分层 | 左栏只展示可创建节点；未实现节点不占视觉位 | 让用户只看到当前可完成的工作 |
| 关系唯一 | 数据依赖由输入选择创建，并自动生成锁定箭头 | 线就是实际输入，避免装饰线和数据线打架 |

工具栏新增“统一尺寸”，用于把旧画布中的已实现节点恢复到 `360 × 360` 基准。手动缩放始终保留，方便媒体结果需要更大预览的场景。

## 信息层级

每张节点卡片采用固定五层，从上到下：

1. **标题栏**：类型图标、可编辑标题、设置入口；不放业务参数。
2. **配置层**：仅在点击 ⚙ 后展开；先显示真实输入引用，再显示模型与参数。
3. **内容层**：输入摘要、文本结果、图片或视频预览；为唯一的可滚动区域。
4. **状态层**：仅运行中/失败时出现，运行态显示进度。
5. **动作层**：一个主动作（运行/生成），取消、重试等作为次动作。

色彩只承担分类提示：聊天蓝、单次处理紫、图片粉、视频琥珀、资产青。正文与卡片边框保持中性灰，避免每个区域同时争夺注意力。

## 功能拆分规范

| 类别 | 节点职责 | 不应放入该节点的能力 |
| --- | --- | --- |
| 资产节点 | 保存、预览和发布某种资产 | AI 生成、媒体处理 |
| 生成节点 | 接收明确输入，调用模型，产出新资产 | 资产库管理、复杂剪辑 |
| 处理节点 | 对已有同类资产执行 op | 每个 op 再做成单独节点 |
| 工具节点 | Split/Merge/预览等确定性组织能力 | 调用生成模型 |
| 模型面板 | 展示服务端公开模型目录、同步选择项 | 保存 API Key、直接测试供应商接口 |

后续创建节点必须先写端口契约和“职责一句话”：若不能用一句话说明输入、输出和产物类型，就先不要新增节点。

## 后续两轮改造

### 第二轮：交互收敛

- 统一所有现有节点（Chat / OneShot / Split / Merge / Image）的五层卡片结构和按钮样式。
- 把左侧面板改为“已可用 / 开发中”两个折叠区，并加入关键词搜索。
- 为依赖箭头增加端口名与类型的弱提示；选中节点时只高亮其上下游。
- 增加对齐、等距分布、自动布局三个非破坏性画布动作。

### 第三轮：运行视图

- 增加右侧“任务与产物”抽屉：只显示运行队列、失败原因、最近产物，不把日志塞入节点。
- 框选运行时按 P0 图依赖拓扑排序；视频/图片任务进入统一队列。
- 对长任务提供“已提交 / 排队 / 运行 / 成功 / 失败 / 已取消”可追溯状态。

## 验收标准

- 新画布中的已实现节点默认同宽同高，标题、按钮和状态位置一致。
- 任何用户可见数据连线都能反查到目标节点的具体输入端口。
- 一个节点默认只显示当前任务的核心信息；参数、日志和开发中能力不干扰创作。
- 模型 Key 不出现在浏览器 localStorage、网络请求体或画布快照中。
