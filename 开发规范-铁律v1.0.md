# 无限画布 · 广告生产工作流 —— 开发规范（铁律 v1.0）

> **文档效力**：本文件是本产品后期所有开发的**唯一权威依据**。标注 **【铁律】** 的条款不可违反，违反即返工；标注 **【规范】** 的条款改动需经评审；标注 **【建议】** 的可灵活处理。
> **修订原则**：每次架构决策后更新本文件并在底部记录修订。任何与本文件冲突的代码或设计，以本文件为准。
> **适用范围**：本规范约束节点体系、关系模型、层级结构、模块划分。技术选型部分（第六部分）为【建议】，非铁律。

---

## 目录

- [第一部分 产品定位与核心模型](#第一部分-产品定位与核心模型)
- [第二部分 层级结构与模块划分](#第二部分-层级结构与模块划分)
- [第三部分 节点体系核心铁律](#第三部分-节点体系核心铁律)
- [第四部分 节点清单与端口契约表](#第四部分-节点清单与端口契约表)
- [第五部分 节点运行机制](#第五部分-节点运行机制)
- [第六部分 技术选型建议](#第六部分-技术选型建议)
- [附录 术语表与修订记录](#附录-术语表与修订记录)

---

## 第一部分 产品定位与核心模型

### 1.1 产品定位【规范】

本产品是一个**无限画布式的广告生产工作流平台**。核心使命：把"AI 松散连接的广告生产过程"抽象成画布上的节点，让创作者能在一张自由画布上发散创作，并把成熟流程固化为可复用的工作流。

**一句话定义**：创作者在自由白板上排布节点做内容创作；成熟的节点组合可一键封装成工作流复用。

### 1.2 双态模型【铁律】

画布上的节点组合同时具备两个状态，用户可在两者间自由切换：

| 状态 | 名称 | 语义 | 默认场景 |
| --- | --- | --- | --- |
| **发散态** | 草稿态 / 创作态 | 节点松散排布，无执行顺序，手动逐个运行，随时调参试错 | 内容创作时的默认状态 |
| **固化态** | 工作流态 | 框选的节点组合被编译成可一键运行的流水线，对外暴露输入/输出端口 | 成熟流程复用 |

**切换规则**：
- 发散态 → 固化态：**框选子图 + "封装为工作流"**。系统把选中的节点 + 它们之间的引用关系冻结成一个工作流单元。
- 固化态 → 发散态：**"解封装"**。工作流单元随时展开回发散态继续编辑。

**铁律**：进入画布的默认状态永远是发散态。不存在"进入画布就必须先画一条完整工作流"的约束。这是本产品与扣子等传统工作流引擎的根本区别。

### 1.3 核心张力与解决原则【规范】

本产品要同时满足两类诉求，它们天然存在张力：

- "自由发散创意台 + 多媒体良好呈现"（白板诉求）
- "节点规范运行 + 成熟流程自动化"（工作流诉求）

**解决原则**：画布层优先满足白板诉求（自由多媒体呈现）；节点规范与运行机制满足工作流诉求；两者通过"双态模型"统一。**不为其中一个诉求牺牲另一个**。

---

## 第二部分 层级结构与模块划分

### 2.1 三级层级结构【铁律】

本产品采用**课程 → 章节 → 项目（画布）**三级树形结构。此结构参考团队既有 PPT 项目的成熟实现（已验证的课程/章节/项目 CRUD、拖拽排序、跨层级移动），适配到画布场景。

```
课程 Course
├── 章节 Chapter
│   ├── 项目 Project（= 一张无限画布）
│   └── 项目 Project
├── 章节 Chapter
│   └── 项目 Project
└── 未归类项目（直接挂在课程下，不属任何章节）

独立项目（不归入任何课程）
```

**层级语义**：
- **课程**：顶层容器，对应一个主题/客户/系列（如"某品牌双十一广告系列"）。带封面色、封面图、描述。
- **章节**：课程内的分组（如"信息流广告""开屏广告"）。纯组织用途，不承载内容。
- **项目**：**一张无限画布**，是所有创作和运行的发生地。一个项目 = 一张画布 + 它的节点 + 它的资产。

### 2.2 层级行为规则【规范】

以下规则沿用既有 PPT 项目的成熟设计，直接采纳：

| 操作 | 行为 |
| --- | --- |
| 新建课程 | 随机分配封面色（8 色池），默认名"新建课程" |
| 删除课程 | 删除其下所有章节；**项目解绑变为独立项目**（不删除项目） |
| 删除章节 | **项目解绑，归到课程的"未归类"区**（不删除项目） |
| 移动章节 | 目标课程若有同名章节，自动追加后缀 `(1)(2)...`；章节下项目随之迁移 |
| 拖拽排序 | 课程/章节/项目均支持拖拽排序与跨层级移动 |
| 项目归属 | 项目可挂在章节下、课程未归类区、或完全独立 |

**铁律**：**删除任何容器（课程/章节）绝不级联删除项目**。项目是用户的核心创作成果，只能被用户显式单独删除。

### 2.3 项目（画布）数据模型【规范】

每个项目对应一张画布，数据模型如下：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 形如 `xxx_HHMMSS`，沿用既有格式 |
| `name` | string | 项目名 |
| `description` | string | 描述 |
| `course_id` | string? | 所属课程，null 为独立项目 |
| `chapter_id` | string? | 所属章节，null 归课程未归类区 |
| `sort_order` | int | 排序 |
| `canvas_snapshot` | json | 画布快照（tldraw snapshot：节点位置/连线/UI 状态） |
| `nodes_state` | json | 各节点的配置与运行结果缓存 |
| `assets_ref` | json | 项目引用的资产清单（引用，非拷贝） |
| `model_config_id` | string | 该项目默认使用的 Model 配置集 |
| `storage_dir` | path | 项目独立文件目录（存上传的资产、生成的产物） |
| `created_at / updated_at` | datetime | 时间戳 |

**注意**：相比既有 PPT 项目，**移除 `current_step / step_status`**——因为画布是非线性的，不存在"当前步骤"。新增 `canvas_snapshot / nodes_state / assets_ref / model_config_id`。

### 2.4 项目复制【铁律】

**项目必须支持复制**。复制规则：

- **默认浅复制**：复制画布快照 + 节点配置；**运行结果清空**（复制的项目是干净的初始态）；**资产以引用方式共享**（不拷贝文件，节省存储）。
- **可选深复制**：勾选"连同素材一起复制"时，资产文件一并拷贝，新项目完全独立可单独演化。

**铁律**：复制后的项目必须是一个全新的、独立的画布，修改不影响源项目（即使浅复制，也只共享资产文件本身，节点配置与运行结果独立）。

### 2.5 四大功能模块【铁律】

整个产品由四个模块构成：

| 模块 | 职责 | 归属层级 |
| --- | --- | --- |
| **① 课程管理模块** | 课程/章节/项目的 CRUD、排序、移动、复制；提供树形导航 | 全局（画布之外） |
| **② Model 设定模块** | 管理模型列表（provider/key/参数），支持多模型配置与切换 | 项目级（每项目可独立配置，可继承全局默认） |
| **③ 节点资产模块** | 文本/图片/视频/音频/文档资产的上传、入库、预览、复用 | 画布内（作为节点）+ 项目级资产库 |
| **④ 生成节点模块** | 图片生成、视频生成的任务调度与结果呈现 | 画布内（作为节点） |

**模块② Model 设定模块细则【铁律】**：
- 模型抽象：`{ provider, apiKey, baseUrl, modelId, type: chat|image|video|audio|embedding, defaultParams }`
- 一个项目可配置多个模型，节点在设置里下拉切换。
- 用途分类：聊天/单次处理用 `chat` 类；图片生成用 `image` 类；视频生成用 `video` 类。
- **首轮接入【铁律】**：图片生成 = 中转站 Image 2（ChatGPT Image）；视频生成 = MiniMax H3 + Seedance 2.0。暂不接入 ComfyUI。

---

## 第三部分 节点体系核心铁律

> 这是整个产品最容易失控的部分，以下四条铁律决定了系统能否线性可维护。**每遇到一个新功能，先用这三节的判据归类，再动手实现。**

### 3.1 节点 vs 功能(op) 判据【铁律】

**核心法则**：判断一个能力是"独立节点"还是"某个节点的功能(op)"，只看一件事——**它是在变换现有资产，还是在创造新资产**。

- **变换**（切分、截帧、转格式、转深度、截取片段）→ 是某个媒体处理节点的 **op（功能）**，不是独立节点。
- **创造**（生成图片、生成视频、生成音频）→ 是**独立节点**，因为它要从无到有产出，依赖 AI 推理。

**三问法则**（边界情况用这三问归类）：

| 问题 | 答"是" | 答"否" |
| --- | --- | --- |
| 1. 核心是确定性变换（给定输入产出唯一）？ | → 倾向 op | → 看下一题 |
| 2. 从零创造新内容（需要 prompt / 从文本生图）？ | → **独立节点** | → 倾向 op |
| 3. 有独特交互范式（画标注框、涂蒙版）？ | → **独立节点** | → op |

**灰色地带的处理【铁律】**："对已有素材做 AI 变换"（视频转深度、视频转白膜、图片抠背景、图片转线稿）——**归为 op**，但该 op 的实现内部会调用 AI 模型。架构上保持 op，体验上创作者只看到一个熟悉的节点，不为"要不要调模型"纠结。

**节点总数约束【铁律】**：节点类型控制在 **20 种上下**。处理类能力一律通过给处理节点加 op 实现，**禁止为每个处理能力新建节点**。能力无限扩展，节点拓扑不变。

### 3.2 三种关系语义【铁律】

**禁止把节点间关系混为一谈**。必须明确区分为三种性质完全不同的关系：

| | 数据依赖 | 流程连线 | 呈现绑定 |
| --- | --- | --- | --- |
| **是什么** | B 引用 A 的输出当输入 | 用户在画布上画的线 | 预览节点"订阅"某来源实时镜像显示 |
| **谁建立** | 用户在 B 的输入框选引用源 | 用户在画布上拖线 | 用户在预览节点设置里选监视对象 |
| **强度** | 松散，断了不影响 A | 发散态下是装饰性的 | 极轻量，只读 |
| **生效状态** | 发散态 + 固化态 | 主要在固化态 | 发散态 |
| **举例** | 图片生成引用剧本当参考 | 框选 3 节点封装工作流 | 文本预览节点绑定聊天输出 |

**铁律**：
- 数据依赖是**唯一传递数据**的关系。流程连线和呈现绑定都不传数据。
- 三种关系互不干扰，各走各的通道。
- 用户之前纠结的"聊天节点和文本节点有没有关系"，本质是把三种关系混为一谈：它们在数据依赖上无关，但可通过呈现绑定关联。

### 3.3 类型契约【铁律】

**关系不由"节点对节点"建立，而由"端口类型契约"涌现。** 这是避免 O(N²) 关系爆炸的唯一方法。

**资产类型枚举（铁律）**：

```
Text        纯文本/Markdown
Prompt      提示词（Text 的语义子类）
ChatHistory 对话历史（结构化）
Image       图片
Video       视频
Audio       音频
Doc         文档（Word、PDF）
Structured  结构化 JSON（如资产提取结果）
```

**铁律**：每个节点只声明"我吃什么类型、我产出什么类型"，**不认识任何其他节点**。节点之间能否发生关系，由类型是否匹配自动涌现。加一个新节点 = 加一份端口声明，**不修改任何现有节点**。

**隐式类型转换规则【规范】**（让该有关系的地方自动能连）：
- `Prompt ↔ Text`（互转）
- `ChatHistory → Text`（展平成转录文本）
- `Image → Image[]`、`Prompt → Prompt[]`（单值包数组）
- `Structured → Text`（序列化展示）

### 3.4 数组扇出【铁律】

这是支撑"批量生产"（如 10 个分镜分别生成）的核心机制，由两部分配套组成，缺一不可：

**① Split / Merge 节点【铁律】**：
- **Split（拆分）**：输入 `Text`，按分隔符（默认换行 / `===` / `---`，可自定义）拆成 `Text[]`。
- **Merge（合并）**：输入 `Text[]`，按连接符合并回 `Text`；也可合并 `Image[]`→拼图、`Video[]`→拼接。

**② 下游自动 map【铁律】**：
- 所有节点统一遵守一条规则：**输入是单值就跑一次，输入是数组就自动对每个元素各跑一次（map 语义）**。
- 不为每个节点单独写批量逻辑；数组扇出是所有节点共享的行为。

**典型流水线（分镜批量生产）**：
```
[剧本 Doc] → [单次处理·分镜生成] → [Split 按 === 拆] → [图片生成 自动map×10] → [视频生成 自动map×10] → [Merge 合成]
```
摆 6 个节点，自动产出 10 套分镜的图文视频。

**禁止【铁律】**：不为分镜拆分做代码节点。拆分是高频通用操作，必须做成"填个分隔符就完事"的内置 Split 节点。代码节点只作为后期给极客用户的逃生通道，绝不是批量处理的默认方案。

---

## 第四部分 节点清单与端口契约表

### 4.1 节点全景清单【铁律】

节点分四大类，总计约 19 种（符合 20 种约束）：

```
【一、生成类节点】（独立节点，从零创造，依赖 AI）
  1. 聊天节点 ChatNode
  2. 单次处理节点 OneShotNode
  3. 图片生成 ImageGen
  4. 视频生成 VideoGen
  5. 标注修图 AnnotateEdit（独特交互：画标注框指哪打哪）

【二、处理类节点】（按媒体归类，内含 op，变换不创造）
  6. 图像处理 ImageProcess
  7. 视频处理 VideoProcess
  8. 音频处理 AudioProcess

【三、资产节点】（内容载体，本体一致呈现不同）
  9.  文本资产 TextAsset
  10. 图片资产 ImageAsset
  11. 视频资产 VideoAsset
  12. 音频资产 AudioAsset
  13. 文档资产 DocAsset（Word/PDF）

【四、工具 / 呈现节点】
  14. Split 拆分节点
  15. Merge 合并节点
  16. 文本预览节点 TextPreview（呈现绑定）
```

### 4.2 处理类节点的 op 注册表【铁律】

处理能力一律以 op 形式挂载到对应媒体处理节点。**加新处理能力 = 加一个 op，不新增节点。**

**图像处理 ImageProcess 的 op**：

| op | 输入 | 输出 | 参数 | 是否需 AI |
| --- | --- | --- | --- | --- |
| `split` 切分 | Image | Image[] | parts(几等份) | 否 |
| `crop` 裁剪 | Image | Image | box(矩形) | 否 |
| `resize` 缩放 | Image | Image | w, h | 否 |
| `format` 转格式 | Image | Image | target(png/jpg/webp) | 否 |
| `removeBg` 抠背景 | Image | Image | — | **是** |
| `toLineArt` 转线稿 | Image | Image | — | **是** |

**视频处理 VideoProcess 的 op**：

| op | 输入 | 输出 | 参数 | 是否需 AI |
| --- | --- | --- | --- | --- |
| `trim` 裁剪片段 | Video | Video | start, end（时间段） | 否 |
| `extractFrame` 截取某帧 | Video | Image | time（时间点）或 frameIndex | 否 |
| `extractAudio` 提取音轨 | Video | Audio | start?, end?（可截某段的音频） | 否 |
| `subclip` 变速 | Video | Video | speed | 否 |
| `toDepth` 转深度 | Video | Video | — | **是** |
| `toWhiteMembrane` 转白膜 | Video | Video | — | **是** |

**音频处理 AudioProcess 的 op**：

| op | 输入 | 输出 | 参数 | 是否需 AI |
| --- | --- | --- | --- | --- |
| `trim` 截取片段 | Audio | Audio | start, end | 否 |

> 注：从视频提取音频归在 VideoProcess.extractAudio（输入是 Video），不归 AudioProcess。AudioProcess 只处理已经是 Audio 的输入。

**文档处理【铁律】**：**不做文档二次处理节点**。文档资产仅用于上传和作为输入被引用（如单次处理节点读取 Word 剧本）。

### 4.3 完整端口契约表【铁律】

这是所有节点的输入输出声明，**是节点关系体系的唯一规格基准**：

| 节点 | 输入端口 | 输出端口 |
| --- | --- | --- |
| **聊天节点** | context: Text?（可选上下文） | ChatHistory（可转 Text） |
| **单次处理** | input: Text / Doc / ChatHistory | Structured（可转 Text） |
| **图片生成** | prompt: Prompt 或 Prompt[], refs: Image 或 Image[]? | Image 或 Image[] |
| **视频生成** | prompt: Prompt 或 Prompt[], refs: Image 或 Image[]? | Video 或 Video[] |
| **标注修图** | image: Image, annotation: 标注数据 | Image |
| **图像处理** | image: Image 或 Image[] | 取决于 op |
| **视频处理** | video: Video 或 Video[] | 取决于 op（trim→Video, extractFrame→Image, extractAudio→Audio） |
| **音频处理** | audio: Audio 或 Audio[] | Audio 或 Audio[] |
| **文本资产** | — | Text |
| **图片资产** | — | Image |
| **视频资产** | — | Video |
| **音频资产** | — | Audio |
| **文档资产** | — | Doc |
| **Split** | text: Text | Text[] |
| **Merge** | items: Text[] 或 Image[] 或 Video[] | Text 或 Image 或 Video |
| **文本预览** | text: Text | （呈现，无输出） |

### 4.4 节点统一抽象【规范】

每个节点在数据层统一抽象为：

```ts
{
  id: string,           // 唯一标识
  type: string,         // 节点类型（对应清单）
  position: {x, y},     // 画布坐标
  config: {},           // 节点配置（如聊天节点的系统提示词/模型/温度）
  inputs: [],           // 输入引用（数据依赖，指向其他节点输出）
  outputs: {},          // 输出缓存（上次运行结果）
  state: 'idle'|'running'|'done'|'error'  // 运行态
}
```

此抽象借鉴扣子 coze-studio 的 `WorkflowNodeRegistry { type, meta, formMeta }` 注册表范式（见附录参考资料），但剥离其"强制单一 Start/End"约束。

### 4.5 各类节点的设计要点【规范】

**聊天节点 ChatNode**：
- 形态：画布上对话卡片，含消息流 + 输入框 + 顶部设置（⚙️）。
- 设置项【铁律】：系统提示词、模型（来自 Model 模块）、最大输出长度、温度。（与最初需求一致）
- 对话历史持久化到节点 state，节点本身就是"结果沉淀"。
- 用途：统一页面聊天并沉淀结果。

**单次处理节点 OneShotNode**：
- 形态：输入区 + 一键执行 + 输出区（结构化展示）。
- 语义：无状态 one-shot LLM 调用，无多次交互。
- 配置：prompt 模板（含变量插槽）、模型、温度、输出 schema。
- 用途【铁律】：**分镜生成、资产提取、文案润色、翻译等，都是这一个节点换不同 prompt 模板**，不为每种用途单独做节点。原则是"节点类型少而精，prompt 模板多"。

**资产节点**：
- 设计原则【铁律】：**内容与渲染解耦**——内容（asset blob/url/text）+ 一组预览器（文本/图片/音频/视频/文档）。换 logo 只换 contentRef，引用关系不变。
- 上传 → 存项目独立目录 → 落 asset。

**生成节点（图片/视频）**：
- 异步任务节点：prompt + 参考输入 → 调模型 → 进度/缩略图 → 成功后呈现为可缩放媒体 shape。
- 图片生成对标 Cowart 的"AI 图片框"（按框位置/比例生成并替换）。
- 视频生成支持长耗时任务（进度条 + 任务队列 + 失败重试）。
- 支持重生成、基于结果再编辑、标注修图（对标 Cowart 指哪打哪）。

---

## 第五部分 节点运行机制

### 5.1 三种运行粒度【铁律】

| 运行方式 | 触发 | 适用状态 | 说明 |
| --- | --- | --- | --- |
| **单节点运行** | 点节点上的"运行" | 发散态 | 只跑这一个节点；输入来源优先级：①显式引用的上游输出 ②节点缓存 ③用户手填 |
| **框选运行** | 选中多个节点 → "运行所选" | 发散态 | 按节点间引用关系算出拓扑序，无依赖则并行，临时跑一遍 |
| **工作流运行** | 运行封装好的工作流单元 | 固化态 | 一键运行整条流水线，按封装时冻结的顺序执行 |

**铁律**：不存在"必须从某个开始节点跑到结束节点"的强制。任何节点、任何子图都可独立运行。

### 5.2 工作流封装与解封装【铁律】

**封装**：框选子图 + "封装为工作流" → 系统把这组节点 + 它们的引用关系冻结成一个工作流单元，对外暴露输入/输出端口。以后一键运行，甚至可拖到别的画布当"子工作流节点"复用。

**解封装**：工作流单元随时可展开回发散态，继续调参、增删节点、再固化。

**封装数据结构【规范】**：
```ts
WorkflowUnit {
  id, name,
  nodes: [...]        // 封装时的节点快照
  edges: [...]        // 封装时的引用关系
  inputs: [...]       // 对外暴露的输入端口
  outputs: [...]      // 对外暴露的输出端口
  modelConfigRef      // 运行时使用的模型配置
}
```

### 5.3 异步任务状态机【规范】

生成节点（图片/视频）涉及长耗时任务，必须实现异步任务状态机：

```
pending → running → done
                   ↘ failed → 可重试
支持：pause / continue / cancel
```

- 任务队列统一管理；进度可轮询或 WebSocket 推送。
- 结果缓存到节点 state，节点重渲染显示缩略图/播放器。
- 失败可一键重试；支持基于上次结果的再编辑再生成。

### 5.4 输入解析优先级【铁律】

任何节点运行时，其输入按以下优先级解析：
1. **显式引用**：节点配置里引用的上游节点输出（最高优先级）
2. **节点缓存**：上次运行的输出（引用缺失时的兜底）
3. **用户手填**：用户在节点上手动填入的值（调试用）

这条规则保证了"框选中间节点单独跑"可行——即使上游没跑，也能用手填或缓存撑过去。

---

## 第六部分 技术选型建议

> 本部分为【建议】，非铁律。可随技术验证调整，但调整需记录原因。

### 6.1 画布引擎选型

基于"扣子可视化呈现差，不适合内容创作"的明确结论，**排除扣子同款引擎 FlowGram.ai 作为画布**。候选：

| 候选 | 许可证 | 适合度 | 说明 |
| --- | --- | --- | --- |
| **tldraw**（首选） | 自定义（生产需 key，开源项目免费） | ★★★★★ | 白板范式，多媒体自由呈现最强；官方有 Workflow/Image Pipeline/Agent/Chat 四个 Starter Kit 直接命中需求 |
| React Flow（备选） | MIT | ★★★ | 节点 DAG 标准；多媒体自由度弱于 tldraw |

**建议**：以 tldraw 为画布地基（多媒体自由呈现是本产品核心），借鉴扣子节点 schema 自研轻量运行内核。若 tldraw 商业许可成本不可接受，退回 React Flow。

**验证方法**：开发前用半天做对比 Spike——分别跑通 tldraw 的 Image Pipeline Starter Kit 和扣子的 workflow demo，凭手感（图片能否自由缩放标注、节点面板好不好写、自定义节点开发量）拍板。

### 6.2 节点规范借鉴

**借鉴扣子 coze-studio 的以下设计**（实证自其源码，见 `.research/coze-studio-节点规范实证.md`）：
- 节点注册表范式 `WorkflowNodeRegistry { type, meta, formMeta }`
- `FormMetaV2` 表单引擎（init/submit 双向转换统一驱动配置面板）
- 变量类型系统（39 种含多媒体类型）
- 引用式数据流（节点输出发布到作用域，下游引用）
- 单节点调试机制（手动填输入即跑）

**剥离扣子的**：强制单一 Start/End、整流运行引擎、企业级资源体系、扣子式呈现。

### 6.3 推荐技术栈

- 前端：React 19 + TypeScript + Vite + tldraw + TipTap（富文本）+ zod（schema 校验）（对标 Cowart 技术栈）
- 节点运行内核：自研轻量调度器（节点 run + 依赖拓扑 + 输出缓存），参考 LangFlow step-by-step 与扣子 `WorkflowNodeDebugV2`。
- 后端：参考既有 PPT 项目的 FastAPI 架构；负责模型中转代理、异步任务队列、资产存储、画布快照持久化。
- 存储：SQLite（沿用既有）+ 项目独立文件目录（沿用既有 project_storage 安全路径设计）。
- 模型接入：中转站统一入口（Image 2）、MiniMax、Seedance 适配器。

### 6.4 MVP 路线图

1. **课程/章节/项目树 + 空画布**：复用既有 PPT 项目的层级 CRUD，接入 tldraw 空画布 + 项目复制。
2. **聊天节点 + 文本资产**：跑通"画布上聊天并沉淀"最小闭环。
3. **单次处理节点 + Split/Merge**：跑通"剧本→分镜拆分→批量引用"。
4. **图片生成节点**：中转站 Image 2，异步任务 + 缩略图（对标 Cowart AI 图片框）。
5. **标注修图 + 图像处理 op**：指哪打哪 + 切分/裁剪等 op。
6. **视频生成节点**：MiniMax H3 / Seedance 2.0，长任务队列。
7. **视频/音频处理 op**：截帧、截片段、提取音轨。
8. **框选运行 + 工作流封装/解封装**：双态切换闭环。
9. **Model 模块完善**：多模型配置与切换。

---

## 附录 术语表与修订记录

### 术语表

| 术语 | 定义 |
| --- | --- |
| 发散态 | 画布默认状态，节点松散排布无执行顺序，手动运行 |
| 固化态 | 节点组合被封装成工作流后的状态，一键运行 |
| 节点 | 画布上的功能单元，分生成类/处理类/资产类/工具类 |
| op | 处理类节点内的功能项（如切分、截帧），不是独立节点 |
| 端口契约 | 节点声明的输入/输出类型，决定它能与谁建立关系 |
| 数据依赖 | B 引用 A 输出当输入的关系（唯一传数据的关系） |
| 呈现绑定 | 预览节点订阅某来源实时镜像显示的关系（只读） |
| 扇出 / map | 输入是数组时，下游节点对每个元素各跑一次 |
| 封装 | 把框选的子图编译成可复用的工作流单元 |

### 修订记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 本轮 | 首版。确立双态模型、层级结构、节点判据、三种关系、类型契约、数组扇出、节点清单、端口契约表、运行机制 |

### 参考资料实证来源

- **层级结构**：参考既有 PPT 项目 `course_service.py` / `project_service.py` / `project_storage.py`（课程/章节/项目 CRUD、拖拽排序、跨层级移动、路径安全防护已验证）。
- **节点规范借鉴**：扣子 coze-studio 源码实证，见 `.research/coze-studio-节点规范实证.md`（25+ 真实文件路径）。
- **画布与生成节点参考**：Cowart（基于 tldraw 的 AI 画布插件），技术栈与实现见 `.research/Cowart-README.md`。
- **开源生态全景**：见 `无限画布-开源方案调研报告.md`。
