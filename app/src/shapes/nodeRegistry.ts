// Compatibility facade. Node metadata now has one authoritative source.
export { CATEGORY_LABEL, NODE_DEFINITIONS as NODE_REGISTRY, type NodeCategory, type NodeDefinition as NodeMeta } from './nodeDefinitions'
export const NODE_DRAG_MIME = 'application/x-infinite-canvas-node'
