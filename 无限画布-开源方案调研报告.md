# 无限画布 · 广告生产工作流 —— 开源方案调研报告

> 调研日期：本轮会话。数据来源：GitHub 仓库元数据 / README / 源码目录抓取（实时）。
> 调研目标：为"把一个 AI 松散连接的广告生产工作流，抽象成无限画布上的节点"这件事，发散性地盘点可借鉴的开源方案，并给出具体落地建议。
> 调研立场：**先发散、再收敛；不预设技术路线，但给出带理由的倾向性结论。**

---

## 0. 一句话结论（TL;DR）

- **画布引擎有三个可选地基**（详见第 2 节对比与第 3.5 节新增的扣子实证章节）：
  - **tldraw**（白板范式，多媒体自由呈现最强，官方有 Workflow/Image Pipeline/Agent/Chat 四个 Starter Kit；自定义许可，生产需 key，开源项目可免费）——最贴合你"自由发散创意台"的诉求。
  - **FlowGram.ai**（字节开源 `bytedance/flowgram.ai`，**MIT 许可**，正是扣子 coze-studio 的画布引擎；自由布局节点 + 20 个插件 + 变量引用式数据流 + FormMetaV2 配置面板；原生支持 Image/Video/Audio 等多媒体类型）——若你更看重"扣子那套节点规范 + 运行机制"且想零许可成本直接复用引擎，它是最佳选择，但需剥离其"强制 Start→End"约束。
  - **React Flow (xyflow)**（MIT，节点连线 DAG 的行业标准；LangFlow/Dify/Flowise 底层全用它）——若走"严格工作流"路线的备选，多媒体自由度弱于前两者。
  - **倾向**：多媒体创意优先选 tldraw；要直接复用扣子节点引擎选 FlowGram.ai；要纯 DAG 选 React Flow。三者都是 React+TS 生态，可混搭（如 tldraw 做画布 + 借鉴扣子节点 schema）。
- **生成节点**：直接对接你的中转站 / 闭源模型 API（图片用 ChatGPT Image / Image 2；视频用 MiniMax H3、Seedance 2.0），**先不接 ComfyUI**——这与你的判断一致。
- **节点规范 / 变量系统 / 资源管理**：**借鉴扣子 coze-studio**（Apache-2.0，Go 微服务 + React + DDD）。实证发现：它的节点注册表 `WorkflowNodeRegistry { type, meta, formMeta }`、39 种多媒体变量类型、引用式数据流（`source:'block-output'`）、单节点调试 `WorkflowNodeDebugV2` 都可直接借鉴或复用；但需剥离"强制单一 Start/End"约束。（详见新增第 3.3.1 节实证）
- **现成参考实现**：**Cowart**（MIT，5.6k★）已经用 **tldraw + React19 + MCP SDK** 做出了"AI 图片框 / 标注指哪打哪修图 / AI HTML / AI Slides"，几乎就是你"生成节点 + 资产呈现"的样板，强烈建议精读其源码。
- **流程灵活性**（框选中间节点单独跑）：学 **LangFlow** 的 interactive playground（step-by-step control）+ 扣子的 `WorkflowNodeDebugV2`（单节点注入输入即跑）；用"每个节点自带 run() + 输出缓存 + 可选级联"实现，不强制连线。

---

## 1. 你的需求结构化（先对齐目标）

| 维度 | 你的描述 | 工程化翻译 |
| --- | --- | --- |
| 顶层结构 | 创建项目 → 点进去就是一张无限画布 | 项目 = 1..N 张画布（canvas）的集合 |
| 聊天节点 | 统一页面聊天并沉淀结果；可设系统提示词/模型/最大长度/温度 | 多轮对话节点，带 LLM 参数面板 + 持久化对话历史 |
| 单次处理节点 | 一次性处理，如"从剧本提取资产"，不需多次交互 | 无状态的 one-shot LLM 调用节点，入参=上游引用，出参=结构化数据 |
| 资产节点 | 文本/图片/音频/视频/Word；"本体一致，呈现不同"（如换 logo） | asset 节点：内容载体 + 多种预览器，内容与渲染解耦 |
| 生成节点 | 图片生成（Image 2）/ 视频生成（MiniMax H3、Seedance 2.0） | 异步任务节点：prompt + 参考输入 → 调模型 → 进度/缩略图/重生成 |
| 节点关系 | 松散；聊天节点与文本节点可无关；输出可接"文本预览节点" | **弱连接图**：节点可独立存在；连接是"引用/绑定"而非强制边 |
| 流程运行 | 不必从开始到结束，可框选中间节点让流程跑起来 | 节点级 run + 框选批量 run + 输出缓存 + 可选下游级联 |
| 模块划分 | 项目创建 / Model 设定 / 节点资产 / 生成节点 | 见第 7 节 |

**核心张力**：你要的"自由画布 + 松散节点 + 多媒体良好呈现" 与 传统工作流引擎的"严格 DAG + 强制连线 + 文本/JSON 为主" 是两套范式。**选型要围绕这个张力展开。**

---

## 2. 开源生态全景：两个范式

### 范式 A —— 白板型无限画布（"画布即文档"，自由排布）

| 项目 | Stars | 许可证 | 定位 | 与你的契合度 |
| --- | --- | --- | --- | --- |
| **tldraw** | 49.9k★ | tldraw 自定义许可（开发免费；生产需 license key；开源项目可免费） | 无限画布 SDK，shape 自由排布，支持图片/视频/嵌入/富文本/协作 | ★★★★★ 最契合 |
| Excalidraw | 130k★ | MIT | 手绘风格白板 | ★★ 偏手绘，不适合做工作流 |
| **Cowart** | 5.6k★ | MIT | 基于 tldraw 的"Codex 原生画布插件"，已实现 AI 图片/标注修图/AI HTML/Slides | ★★★★★ 现成样板 |

### 范式 B —— 节点 DAG 工作流引擎（"节点 + 连线 + 数据流"）

| 项目 | Stars | 许可证 | 底层 | 与你的契合度 |
| --- | --- | --- | --- | --- |
| **React Flow (xyflow)** | 38.1k★ | MIT | 自研 | ★★★ 节点连线范式的事实标准 |
| **LangFlow** | 153.5k★ | MIT | React Flow | ★★★★ 有 step-by-step playground（框选运行） |
| **FlowGram.ai**（扣子引擎） | —（bytedance/flowgram.ai） | **MIT** | 自研自由布局引擎 | ★★★★ 扣子同款引擎，多媒体变量+引用式数据流+FormMetaV2，零许可可复用 |
| **Dify** | 153k★ | Apache-2.0（以仓库为准） | React Flow | ★★★ 企业级 agentic workflow + RAG，偏重 |
| Flowise | 55.4k★ | Apache-2.0（以仓库为准） | React Flow | ★★★ LangChain 可视化 |
| n8n | 201.3k★ | Sustainable Use License（fair-code，商用受限，以仓库为准） | 自研 | ★★ 通用自动化，非 AI 原生 |
| **ComfyUI** | 128.6k★ | GPL-3.0（以仓库为准） | 自研画布 | ★★★ 节点式图像/视频生成之王，但你暂不接入 |
| Rete.js | 12.2k★ | MIT | 自研 | ★★★ 可视化编程框架，可高度定制 |

### 范式 C —— 企业级一站式平台

| 项目 | Stars | 许可证 | 定位 |
| --- | --- | --- | --- |
| **coze-studio**（扣子开源版） | 21.5k★ | Apache-2.0 | Go 微服务 + React + DDD；agent/workflow/插件/知识库/数据库/提示词 一体化 |

> 数据来源：本轮实时抓取自 GitHub API（star/许可证）。少数仓库因 GitHub 限流未取到许可证，标注"以仓库为准"，其余为我已知信息。

**关键观察**：几乎所有主流 AI 工作流引擎（LangFlow / Dify / Flowise）**底层都用 React Flow**——说明 React Flow 是"节点工作流"的行业标准；而**白板创意画布的事实标准是 tldraw**（Google / Shopify / Autodesk / ClickUp / Replit / Runway 都在用）。你要的"多媒体自由呈现"更偏 tldraw，你要的"节点规范/运行"更偏 React Flow 系。**这两者不必二选一——可以用 tldraw 做画布与媒体呈现，用一套自研的轻量节点运行内核做执行。**

---

## 3. 三大候选深入剖析

### 3.1 tldraw（强烈推荐为画布地基）

**为什么是它**：
- 它是"无限画布 SDK"，不是成品白板——你可以完全自定义 shape（自定义节点的外观与交互）。
- **官方直接提供你要的 4 个 Starter Kit**（MIT 许可，可直接 fork）：
  - **Workflow**：拖拽式节点构建器，面向自动化流水线 / 可视化编程 / no-code；
  - **Image Pipeline**：节点式图像生成流水线构建器；
  - **Agent**：能读取、解读、修改画布内容的 AI agent；
  - **Chat / Branching Chat**：画布驱动的 AI 对话（含分支对话，正好对齐你"统一页面聊天并沉淀"）。
- 内置能力直接覆盖你的痛点：图片/视频 shape、富文本（结合 TipTap）、嵌入网页、缩略图、导出、协作（`@tldraw/sync` 自托管）、撤销重做、快照保存加载、Inspector 面板。
- 用它做"生成节点"很自然：生成结果就是画布上一个 asset shape，可缩放、可标注、可迭代——这正是 Cowart 的做法。

**注意点（许可证）**：tldraw 是自定义许可证，**开发阶段完全免费，生产部署需要一个 license key**；若你的产品**本身是开源项目，可免费使用**。对闭源商业产品意味着一笔 license 成本（见第 8 节备选方案）。Cowart 作为开源项目用了 tldraw，证明这条路可行。

**一句话**：tldraw 把"无限画布 + 多媒体 + AI"都做成了 SDK 原语，你不必从零造画布。

### 3.2 Cowart（你的"生成节点 + 资产呈现"现成样板）

**它是什么**：一个面向 Codex 的原生无限画布 widget 插件，**画布能力完全基于 tldraw**，通过 MCP（Model Context Protocol）把"生成/编辑"任务委托给 Codex 这个编程 agent 执行。

**它已经实现的功能**（与你的需求高度重合）：
- **AI 图片框**：选中框 → 输入 prompt + 选参考图 → 按框的位置/比例生成图片并替换 → 这就是你的"图片生成节点"。
- **标注修图（指哪打哪）**：在图上标注 → 截图（原图+箭头+标注文字）发给 Codex → 生成"去掉标注痕迹"的新图放原图旁边 → 这就是你说的"基于 Codex 的指哪打哪修图"。
- **AI HTML**（16:9）：prompt + 参考图 → 生成可运行单文件 HTML，嵌入画布可继续编辑/迭代。
- **AI Slides**：把图片/HTML 组织成演示文稿，支持缩略图预览 + 全屏播放。
- 数据落地：画布快照保存到项目目录 `canvas/pages/<id>/cowart-canvas.json` 与 `assets/`。

**技术栈（实测自其 package.json）**：
`tldraw ^5.1.1` + `react ^19` + `vite ^7` + `@modelcontextprotocol/sdk` + `@tiptap/pm`（富文本）+ `html2canvas`（截图）+ `fractional-indexing`（协作排序）+ `zod`（schema 校验）。前端入口 `src/App.jsx`，画布客户端 `src/cowartClient.js`，快照 `src/canvasSnapshot.js`，MCP 服务端 `mcp/server.mjs`，三个 skill：`cowart-open-canvas / cowart-image-gen / cowart-image-edit`。

**对你最大的价值**：
1. 它证明了 **"tldraw 画布 + 自定义 AI 节点 + 任务执行后端"** 这条架构是跑得通的；
2. 它的"生成结果替换框 / 标注修图 / 富文本 / 快照持久化"实现可以直接抄；
3. 区别在于：Cowart 把执行委托给 Codex（agent），而你要**直接对接模型 API（闭源快速接入）**——把它的"委托 Codex"那一层换成"直连中转站 Image 2 / MiniMax H3 / Seedance 2.0"即可，架构骨架不变。

### 3.3 coze-studio（扣子开源版）—— 节点规范可借鉴，引擎（FlowGram.ai）竟是 MIT 开源

**定位**：字节扣子"开发平台"的开源核心引擎，一站式 AI Agent 可视化开发工具。

**架构**（自 README + 目录抓取）：后端 **Golang 微服务 + DDD**；前端 **React + TypeScript，rush monorepo**。前端 packages 包括：`agent-ide / arch / common / community / components / data / devops / foundation / project-ide / studio / workflow`。核心模块（Feature list）：
- **模型服务**：管理模型列表，集成 OpenAI、火山引擎等（→ 对应你的"Model 设定模块"）；
- **构建 Agent / App / Workflow**（→ 对应你的画布与节点）；
- **开发资源**：插件、知识库、数据库、提示词（→ 对应你的"资产 + 模型抽象"）；
- **API & SDK**：会话、聊天、工作流 OpenAPI + Chat SDK。

**🔴 重大发现：它的画布引擎不是自研，而是字节开源的 FlowGram.ai（MIT 许可）**：前端工作流画布依赖 `@flowgram.ai/free-layout-editor` + 20 个插件（自动布局/对齐/小地图/分组/连线/变量作用域）；另有独立的 `@coze-workflow/fabric-canvas`（Fabric.js 6）只用于"图像画布"节点的图层合成。经 npm registry 核实，`@flowgram.ai/*` 系列包仓库为 **`bytedance/flowgram.ai`，MIT 许可**（见第 3.5 节）。**这意味着：你不必"只借鉴扣子规范"，连扣子用的画布引擎本身都能合法直接复用**（只要剥离扣子强加的"强制首尾"约束）。

**优点（你说的对，实证确认）**：
- **节点规范通用、扩展性强**：节点以统一注册表 `WorkflowNodeRegistry { type, meta, formMeta }` 定义，新增节点类型有清晰范式（详见 3.3.1 实证）；
- **变量系统成熟且原生支持多媒体**：前端 39 种类型（含 Image/Video/Audio/Svg/Ppt 及其数组），数据靠"引用表达式"流动而非连线传值；
- **模型抽象层完善**：多 provider 可切换，正是你"Model 设定模块"要的；
- **支持单节点调试** `WorkflowNodeDebugV2`（手动填输入即可单独跑）——已部分满足"框选中间节点运行"（但粒度是单节点，非任意子图）；
- 资源（插件/知识库/数据库/Prompt）作为可复用单元，工程化程度高。

**缺点（你说的对，实证确认）**：
- **强制单一 Start/End**：每个工作流必须有且仅有一个 Start（`isStart/deleteDisable`，只有输出端口）和一个 End（只有输入端口）——与"松散、不强制首尾"冲突，需改造放开；
- **部分运行粒度有限**：仅支持"单节点"或"整流"，无"框选任意子图批量跑"，需自研（可复用其 input 注入模式扩展）；
- **多媒体呈现偏卡片**：图片/视频在节点里多是 URL 卡片 + 结果面板自动展开，画布自由度不如 tldraw 的可缩放 asset shape；
- **体量大**：Go 微服务 + rush monorepo + Docker，对一个画布产品而言过重（但若只用其前端 workflow 包 + FlowGram 引擎，可裁剪）。

**借鉴策略升级**（因为引擎开源，选项变多了）：
- ✅ **方案甲（轻）**：只用 tldraw 做画布，借鉴扣子的节点 schema、变量类型、模型抽象层，自研运行内核。
- ✅ **方案乙（中）**：直接用扣子的画布引擎 FlowGram.ai（MIT）+ 自定义节点，复用其节点注册表/FormMetaV2/引用式数据流，剥离"强制首尾"。
- ✅ **方案丙（重）**：fork 扣子前端 workflow 包整体，裁剪成你的画布产品。
- ❌ 不建议：照搬扣子全套（Go 后端 + 企业级资源体系）。

---

### 3.3.1 【实证】扣子节点规范到底长什么样（真实源码）

> 子代理通过 `git clone --filter=blob:none --sparse` 拉取真实代码逐文件阅读得出，非推测。完整报告见 `.research/coze-studio-节点规范实证.md`。

**(1) 节点 = 注册表项 `WorkflowNodeRegistry`**（`base/src/types/registry.ts`）：

```ts
export interface WorkflowNodeRegistry {
  type: StandardNodeType;                  // 数字字符串枚举，如 '3'=LLM、'16'=ImageGenerate
  meta: NodeMeta;                          // 尺寸/端口/数据路径
  variablesMeta?: WorkflowNodeVariablesMeta; // outputs/inputs 在 DTO 中的路径
  formMeta: FormMetaV2;                    // FlowGram 表单引擎，驱动配置面板
}
export interface NodeMeta {
  isStart?; isNodeEnd?; deleteDisable?;    // 节点约束位
  nodeDTOType; size?: { width, height };
  defaultPorts?: any[]; useDynamicPort?: boolean;  // 端口（含动态端口）
  outputsPath?; inputParametersPath?; batchPath?;  // DTO 数据定位
}
```

每个节点的配置面板用 **`FormMetaV2<FormData>`** 统一驱动（这是 FlowGram 的精髓）：

```ts
export const CODE_FORM_META: FormMetaV2<FormData> = {
  render: () => <FormRender />,              // 配置面板 React 渲染
  validate: { ... },                         // 校验
  effect: { outputs: provideNodeOutputVariablesEffect }, // 把输出发布成可用变量
  formatOnInit: transformOnInit,             // 后端DTO → 前端FormData
  formatOnSubmit: transformOnSubmit,         // 前端FormData → 后端DTO
};
```

**内置节点（约 60 种，摘录与你的需求强相关的）**：

| 节点 | type | 与你的对应关系 |
| --- | --- | --- |
| LLM 大模型 | `'3'` | ≈ 你的"聊天节点 + 单次处理节点"底层 |
| ImageGenerate 图像生成 | `'16'` | ≈ 你的"图片生成节点"（含 prompt/ratio/model/references） |
| ImageCanvas 图层合成 | `'23'` | Fabric.js，图层合成——可参考做"合成节点" |
| Code 代码 | `'5'` | 转换/处理类节点 |
| If 条件判断 | `'8'` | `useDynamicPort`，动态分支端口 |
| Plugin/Http | `'4'`/`'45'` | 接外部 API（可参考做"生成节点适配器"） |
| Start/End | `'1'`/`'2'` | 强制首尾（你要剥离的部分） |

**(2) 变量系统（原生多媒体）**：
- 前端 `ViewVariableType` 共 39 种：String/Integer/Boolean/Number/Object + **Image/File/Doc/Code/Ppt/Txt/Excel/Audio/Zip/Video/Svg/Voice** + 18 种 `Array<X>`。
- 后端 FDL：`string/integer/float/boolean/image/object/list/time` + `AssistType`（file/image/doc/.../video/svg/voice）补语义。
- **数据靠"引用表达式"流动，不靠连线传值**：连线只表拓扑；引用结构 `RefExpression.content` 有三种 source：
  - `{ source:'variable', name }` —— 当前节点/全局变量
  - `{ source:'block-output', blockID:'1002', name:'result' }` —— 引用某节点输出
  - `{ source:'global_variable_xxx', ... }` —— 全局变量
- 每个节点把输出发布到命名空间 `/node/outputs`（`provide-node-output-variables`），下游输入框的表达式编辑器里就能选到上游输出——**非常适合"一张图被文案/合成/审核多处复用"的广告场景**。

**(3) 运行机制（执行在 Go+Eino 后端）**：
- 整流试运行：`WorkFlowTestRun({input, commit_id})`，从 Start 跑到 End。
- **单节点调试**：`WorkflowNodeDebugV2({node_id, input, batch, setting})`，执行态 `isSingleMode:true`，轮询进度。**手动填输入即可单独跑某节点**——这是你要的"中间节点单独运行"的现成范式。
- 生命周期：`pause/continue/cancel`；节点状态 Running/Success/Fail 驱动连线高亮；结果面板默认"展开所有 image 流"。

**(4) 画布渲染**：FlowGram.ai free-layout-editor（自由布局节点引擎）+ 扣子自叠的渲染层（背景/悬停/连线/快捷键层 + 端口拉线渲染 `WorkflowPortRender`）。端口是可拖拽圆点，输出端口 `onMouseDown` 开始拉线，输入端口不可拉。

**实证结论**：扣子的节点规范工程质量很高，尤其是"注册表范式 + FormMetaV2 双向转换 + 引用式多媒体数据流 + 单节点调试"——这套设计可以直接拿来作为你"无限画布广告生产"的节点内核蓝本，无论你最终用 tldraw 还是 FlowGram.ai 做画布。

### 3.4 （补充）React Flow 系：LangFlow 的"框选运行"最值得学

- LangFlow（MIT）：可视化构建 + **interactive playground（step-by-step control）**——这正是你要的"框选中间节点让流程跑起来"的产品形态；底层 React Flow。
- 如果最终画布选了 tldraw 或 FlowGram.ai，**运行内核的设计仍可参考 LangFlow 的"节点单步运行 + 中间状态可见"** 思路（与扣子 `WorkflowNodeDebugV2` 异曲同工）。

### 3.5 （新增）第三个画布地基：FlowGram.ai（扣子的引擎，MIT 开源）

**这是什么**：字节开源的自由布局节点引擎 `bytedance/flowgram.ai`，**MIT 许可**，正是扣子 coze-studio 前端工作流的画布底座（经 npm registry 核实：`@flowgram.ai/free-layout-editor` 最新版 1.0.14，license MIT，repo `github.com/bytedance/flowgram.ai`）。

**能力**（源自扣子依赖的 20 个插件）：
- 自由布局节点编辑器（`free-layout-editor`）+ 自动布局/对齐/小地图/分组/层级/连线插件；
- **变量作用域系统**（provider/consumer）——节点输出发布到命名空间供下游引用，天然支持多媒体类型；
- **`FormMetaV2` 表单引擎**——配置面板用 init/submit 双向转换统一驱动（见 3.3.1）；
- 完整的节点注册、端口渲染、连线交互。

**对你的价值**：如果你认同"扣子那套节点规范 + 运行机制通用性好、扩展性强"（你的原话），但又不想吃透 tldraw 的 shape 自定义体系——那么 **直接用 FlowGram.ai 做画布引擎是零许可成本、且能直接复用扣子节点范式** 的最优解。代价是要吃透 FlowGram 的插件体系，且默认是"节点+连线"范式（多媒体自由呈现不如 tldraw 的自由 shape）。

**vs tldraw 的取舍**：
- 要 **白板级的自由多媒体呈现（图片/视频任意缩放标注、自由排版）** → 选 **tldraw**；
- 要 **直接复用扣子节点引擎 + 引用式数据流，且零许可成本** → 选 **FlowGram.ai**；
- 两者都是 React+TS、都支持自定义节点，技术上也可"tldraw 做画布外观 + 借鉴扣子节点 schema 自研内核"混搭。

---

## 4. 选型决策矩阵

| 决策点 | 选项 | 倾向结论 | 理由 |
| --- | --- | --- | --- |
| **画布引擎** | A) tldraw（白板，多媒体自由） / B) FlowGram.ai（扣子引擎，MIT，节点规范可直接复用） / C) React Flow（节点DAG标准） / D) 自研 | **按优先级**：要白板级自由多媒体呈现→**tldraw**；要直接复用扣子节点引擎+零许可成本→**FlowGram.ai**；两者都是 React+TS，也可 tldraw 外观+借鉴扣子 schema 混搭。详见第 3.1 / 3.5 节。 | 你的核心张力是"自由发散创意台（白板）" vs "节点规范运行（DAG）"。tldraw 最贴白板诉求且有官方 Starter Kit；FlowGram.ai 让你能合法复用扣子整套节点范式且 MIT 免费。React Flow 是纯 DAG 备选。 |
| **执行后端** | A) 直连闭源模型 API / B) Agent 委托（Cowart式）/ C) 接入 ComfyUI | **A：直连模型 API** | 你明确要"闭源快速接入、先不接 ComfyUI"。图片→中转站 Image 2；视频→MiniMax H3 / Seedance 2.0。把 Cowart 的"委托 Codex"换成"直连 API"即可。 |
| **流程模型** | A) 严格 DAG / B) 松散弱连接图 | **B：松散弱连接图** | 节点可独立存在；连接是"引用/绑定"而非强制边；支持单节点 run、框选批量 run。借鉴 LangFlow 的 step-by-step 思路。 |
| **节点规范** | 自研 / 借鉴扣子 / 借鉴 ComfyUI | **借鉴扣子 schema**（`WorkflowNodeRegistry + FormMetaV2`）；运行内核看画布选择：若用 FlowGram.ai 则直接复用其调度，若用 tldraw 则自研轻量调度器 | 扣子的节点注册表、39 种多媒体变量类型、引用式数据流、单节点调试工程质量高；详见 3.3.1 实证。 |
| **是否要协作** | 单机 / 多人协作 | MVP 单机，预留协作 | tldraw 有 `@tldraw/sync` 自托管协作，未来可平滑升级。 |

---

## 5. 针对你四类节点的落地设计

> 统一抽象：每个节点 = `{ id, type, position, config, inputs, outputs, state }`。
> 在 tldraw 里，每个节点是一个**自定义 shape**（带 header + body 渲染），可自由拖拽、缩放、框选；节点间用"引用边"（可选连线）表达数据依赖，**不连线也能跑**。

### (1) 聊天节点 ChatNode
- **形态**：画布上一个对话卡片，含消息流 + 输入框 + 顶部设置（⚙️）。
- **设置项**：系统提示词、模型（来自 Model 模块）、最大输出长度、温度。（与你的描述完全一致。）
- **沉淀**：对话历史持久化到节点 state，节点本身就是"结果沉淀"。
- **输入**：可引用上游文本/资产节点作为"上下文"（弱连接）。
- **实现参考**：tldraw 的 Note/Frame shape + TipTap 富文本 + Chat Starter Kit。

### (2) 单次处理节点 OneShotNode
- **形态**：输入区 + 一键"执行" + 输出区（结构化展示）。
- **语义**：无状态 one-shot LLM 调用。典型用途：剧本→资产提取（输出结构化 JSON/表格，可再被下游引用）。
- **配置**：prompt 模板（含变量插槽）、模型、温度、输出 schema（用 zod/JSON Schema 校验，借鉴扣子变量类型）。
- **输入**：上游节点的输出（文本/JSON）。

### (3) 资产节点 AssetNode（文本/图片/音频/视频/Word）
- **形态**：内容载体 + 适配的预览器。你说得对——"本体一致，呈现不同"，所以设计成：**内容（asset blob / url / text）+ 一组预览器（文本/图片/音频/视频/文档）**，内容与渲染解耦（换 logo 不影响引用关系）。
- **上传**：本地文件 → 上传到对象存储/本地 → 落 asset。
- **实现参考**：Cowart 的 asset shape + html2canvas/缩略图；tldraw 的 image/video/embed shape。

### (4) 生成节点 GenNode（图片 / 视频）
- **图片生成**：prompt + 参考图（可引用画布上的 AssetNode）→ 调中转站 **Image 2** → 异步返回 → 缩略图占位 → 成功后替换为可缩放图片 shape；支持"重生成 / 基于结果再编辑 / 标注修图（指哪打哪，学 Cowart）"。
- **视频生成**：prompt + 首尾帧/参考 → 调 **MiniMax H3** 或 **Seedance 2.0** → 长耗时任务（进度条 + 任务队列）→ 返回后在画布以 video shape 呈现，支持播放/缩略图。
- **关键工程点**：异步任务状态机（pending/running/done/failed）、任务队列、轮询/Webhook、结果缓存到节点 state。

### 节点关系（弱连接）
- 不强制连线。任一节点可"引用"另一节点的输出（如聊天节点引用某文本资产为上下文；单次处理节点引用剧本资产为输入；生成节点引用图片资产为参考图）。
- 用 tldraw 的 **binding / 箭头** 表达可视化连接，但**断开连接不影响节点独立存在**。
- "输出接一个文本预览节点" = 一个文本 AssetNode 通过引用边接在聊天节点输出后，实时同步显示其最新输出。

---

## 6. 流程灵活性：怎么实现"框选中间节点单独跑"

- 每个节点自带 `run(context)` 方法，**输入来源**优先级：① 显式引用的上游节点输出 → ② 节点缓存的上次输出 → ③ 用户手填。
- **框选运行**：选中多个节点 → 右键"运行所选"→ 按依赖拓扑顺序执行（无依赖则并行）→ 每个节点输出缓存到 state 并实时渲染。
- **可选级联**：节点运行后，可选择"自动运行引用了我的下游节点"（用户开关）。
- 这样既满足你"不必从头到尾"，又保留了"想要自动串联时也能串"的能力。

---

## 7. 四个模块的落地

### 模块 1 · 项目创建模块
- **项目** = 画布集合 + 共享的 Model 配置 + 资产库 + 历史版本。
- 新建项目 → 进入空画布 → 左侧资产库 / 顶部节点工具栏 / 右侧属性面板（借鉴扣子 studio 的布局，但画布主导）。
- 持久化：画布快照（tldraw snapshot）+ 节点 state + 资产引用表，存本地或后端 DB。

### 模块 2 · Model 设定模块（进入每个画布都可配）
- **模型抽象层**（借鉴扣子模型服务）：`provider / apiKey / baseUrl(中转站) / modelId / 类型(chat|image|video|embedding) / 默认参数`。
- 支持配置多个模型并命名，节点设置里下拉切换。
- 区分用途：聊天/单次处理用 chat 类；图片生成用 image 类（Image 2）；视频生成用 video 类（MiniMax H3 / Seedance 2.0）。

### 模块 3 · 节点资产模块
- asset registry：文本 / 图片 / 音频 / 视频 / Word（/PDF）。
- 统一 asset 协议：`{ id, kind, contentRef, meta, previewers }`，内容与渲染解耦。
- 上传、入库、跨画布复用、版本（换 logo 即换 contentRef，引用不变）。

### 模块 4 · 生成节点模块
- **image-gen 适配器**：中转站 Image 2（ChatGPT Image）。
- **video-gen 适配器**：MiniMax H3、Seedance 2.0（可多选，按模型走对应 API）。
- 适配器统一接口：`submit(params)→taskId / poll(taskId)→result / cancel(taskId)`，未来扩展其他模型零成本。

---

## 8. 推荐技术栈与 MVP 路线图

### 推荐技术栈（MVP）
- 前端：**React 19 + TypeScript + Vite + tldraw + TipTap（富文本）+ zod（schema）**（直接对标 Cowart 技术栈）。
- 节点运行内核：自研轻量调度器（节点 run + 依赖拓扑 + 输出缓存），思路参考 LangFlow step-by-step。
- 后端：Node.js（或 Go）轻量服务——负责模型中转代理、异步任务队列、asset 存储、画布快照持久化。
- 存储：本地/对象存储（asset）+ DB（项目/画布/节点 state/任务）。
- 模型接入：中转站统一入口（Image 2）、MiniMax、Seedance 适配器。

### MVP 路线图（建议分 4 步）
1. **画布骨架 + 聊天节点 + 文本资产**：tldraw 接好，自定义 ChatNode + TextAssetNode，跑通"画布上聊天并沉淀"。对应你的聊天节点 + 文本资产。
2. **单次处理节点 + 图片生成节点**：OneShotNode（剧本→资产提取）+ ImageGenNode（中转站 Image 2），含异步任务与缩略图。对标 Cowart 的 AI 图片框。
3. **视频生成节点 + 标注修图**：VideoGenNode（MiniMax H3 / Seedance 2.0）+ 图片标注修图（对标 Cowart 指哪打哪）。
4. **框选运行 + 弱连接 + Model 模块完善**：节点 run 调度、框选批量运行、引用边、多模型切换、协作预留。

---

## 9. 风险与备选方案

| 风险 | 说明 | 备选 |
| --- | --- | --- |
| tldraw 商业许可成本 | 闭源产品生产部署需 license key | 备选 **FlowGram.ai (MIT)**（扣子同款引擎，零许可成本且可直接复用节点范式）；或 **React Flow (MIT)**；或保持产品开源以免费使用 tldraw。 |
| 视频生成长耗时 | MiniMax/Seedance 单任务可能数十秒~分钟 | 异步任务队列 + 进度节点 + 失败重试 + 结果缓存。 |
| 模型中转站依赖 | 中转站波动/限流 | provider 抽象层 + 多源 fallback；图片可备 OpenAI 直连。 |
| ComfyUI 暂不接入 | 你明确先不接 | 预留适配器接口，未来可加 comfyui-api 适配器，零侵入。 |
| 多媒体性能 | 大量图片/视频 shape 卡顿 | tldraw 有 culling/performance 机制；缩略图 + 懒加载 + 虚拟化。 |
| FlowGram 学习曲线 | 要吃透其插件体系（20 个插件） | 可先用扣子的 workflow 包做参照实现，或退回到"tldraw + 借鉴扣子 schema 自研内核"。 |

---

## 10. 直接可执行的下一步（建议）

1. **做一次"画布引擎对比 Spike"**（半天即可拍板）：分别跑通 ——
   - tldraw 的 Workflow / Image Pipeline Starter Kit（`npx create-tldraw@latest`）：体会白板式自由多媒体呈现；
   - FlowGram.ai 官方 demo（`github.com/bytedance/flowgram.ai`）：体会扣子同款节点引擎与引用式数据流。
   对比维度：① 图片/视频能否自由缩放、标注、排版；② 节点配置面板上手成本；③ 自定义节点的开发量。**凭手感拍板，胜过纸面分析。**
2. **精读 Cowart 源码**（`src/cowartClient.js`、`src/canvasSnapshot.js`、`mcp/server.mjs`、三个 skill）——当作"图片生成 + 标注修图 + 画布持久化"的实现蓝本（若选 tldraw 路线尤其重要）。
3. **照扣子实证设计你的节点 schema**：参考 `.research/coze-studio-节点规范实证.md` 里的 `WorkflowNodeRegistry { type, meta, formMeta }` 与 39 种变量类型，定义你的 ChatNode/OneShotNode/AssetNode/ImageGen/VideoGen 的 schema 与配置面板。
4. **先做 MVP 第 1 步**：选定画布后，做 ChatNode + TextAssetNode，验证"画布上聊天并沉淀"的最小闭环。

---

## 附：本次实时抓取的关键证据

- **coze-dev/coze-studio**：21,478★，Apache-2.0，TypeScript+Go。前端 rush monorepo，含 `workflow` 包；Feature list 含模型服务/Agent/App/Workflow/插件/知识库/数据库/Prompt/API&SDK。部署 `make web` 或 docker-compose，访问 `http://localhost:8888`。
- **🔴 扣子画布引擎 = FlowGram.ai**（实证）：前端 workflow 包依赖 `@flowgram.ai/free-layout-editor@0.1.28` + 20 个插件；经 npm registry 核实 `@flowgram.ai/free-layout-editor`（latest 1.0.14）**license=MIT，repo=`bytedance/flowgram.ai`**。即扣子用的画布引擎是字节 MIT 开源项目，可合法直接复用。
- **扣子节点规范实证**（真实源码）：`WorkflowNodeRegistry { type, meta, formMeta }` 注册表范式；`StandardNodeType` 约 60 种节点（LLM='3'/ImageGenerate='16'/Code='5'/If='8' 动态端口/Start='1'/End='2' 强制首尾）；`ViewVariableType` 39 种含多媒体；引用式数据流 `RefExpression { source:'block-output', blockID, name }`；单节点调试 `WorkflowNodeDebugV2`（`isSingleMode`）。完整报告见 `.research/coze-studio-节点规范实证.md`（含 25+ 真实文件路径）。
- **zhongerxin/Cowart**：5,597★，MIT。技术栈 `tldraw ^5.1.1 + react ^19 + vite ^7 + @modelcontextprotocol/sdk + @tiptap/pm + html2canvas + zod`；画布数据存 `canvas/pages/<id>/`。
- **tldraw/tldraw**：49,879★，自定义许可（生产需 key）。官方 Starter Kit：Workflow / Image Pipeline / Agent / Chat / Branching Chat / Multiplayer / Shader。
- **xyflow/xyflow (React Flow)**：38,069★，MIT。节点式 UI 事实标准（LangFlow/Dify/Flowise 底层均用之）。
- **langflow-ai/langflow**：153,503★，MIT，Python，基于 React Flow。卖点含 interactive playground（step-by-step）、导出 JSON、MCP server。
- 其余（Excalidraw 130k★/MIT、Rete 12.2k★/MIT、Dify、Flowise、n8n、ComfyUI）见第 2 节表格。

> 抓取产物已保存于本项目 `.research/` 目录：`coze-studio-README.md`、`Cowart-README.md`、`tldraw-README.md`、`tldraw-llms.txt`、`react-flow-README.md`、`langflow-README.md`、`dify-README.md`、**`coze-studio-节点规范实证.md`**，可随时查阅。
