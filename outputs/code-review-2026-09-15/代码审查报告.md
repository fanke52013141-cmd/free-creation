# Canvas Studio 代码审查报告与优化建议

> 审查日期：2026-09-15 ｜ 基线：555a77e ｜ 范围：代码同步、安全、数据完整性、执行一致性与工程质量

## 1. 执行摘要

**结论：代码已推送到 GitHub main，但当前版本不应据此被认定为已完成发布验收。** 本轮确认 11 项代码问题，其中 **P1 高优先级 8 项、P2 中优先级 3 项**。最需要先处理的是保存失败后的错误回滚、关窗丢失数据、headless 代码执行隔离，以及桌面与 CLI/MCP 的执行结果不一致。

这 11 项是本轮可证实的问题，不代表整个代码库只有这些问题，也不是从测试失败数量推导得出的漏洞数量。审查为关键链路深入检查，不是对全部源码逐行穷尽证明。

### 关键结果

| 项目 | 结果 | 解读 |
| --- | --- | --- |
| 远端同步 | 本地 HEAD 与服务器 main 均为 555a77ec0d738f5d35c7b1a4d922391f61b93231 | 用户确认后改为将本地 104 个提交推送到远端 |
| 已跟踪文件 | 审查期间未修改业务源码 | 没有强推、回退、额外业务提交或自动修复 |
| TypeScript | 主进程和前端类型检查均通过 | 不等于业务行为正确 |
| 代码规范 | 0 error，129 warnings，退出码 0 | 告警来自 prettier/prettier；不能写成“零问题” |
| 完整测试 | 77 文件；950 项，932 通过、18 失败 | 失败涉及 6 个文件，根因是 SQLite 原生模块 ABI 不匹配 |
| 生产构建 | 单独重试成功 | 初次运行内存分配失败；没有完成安装包与真实桌面验收 |
| 代码发现 | 8 项 P1、3 项 P2 | 与 18 项测试失败分开统计 |

**建议修复顺序：数据保存安全 → 不可信代码隔离 → 跨端执行统一 → 取消/续跑语义 → 测试环境与覆盖率 → 性能和维护性。**

## 2. 同步基线与变更边界

- 工作目录：`D:/software/free-creation`。
- 远端仓库：`https://github.com/fanke52013141-cmd/free-creation.git`。
- 分支：本地 `main`，服务器 `refs/heads/main`。
- 初次获取远端后，本地比远端多 104 个提交，没有已跟踪文件的未提交改动；远端旧头部为 `363f2d142b8fc8f7a13a5a6ef4132648f0332250`。
- 因此没有按最初字面要求回退本地。确认后，用户明确指示“把本地的提交到线上”，本轮按这一最新指示正常推送已有提交。
- 同步后直接查询服务器，main 为 `555a77ec0d738f5d35c7b1a4d922391f61b93231`，与本地 HEAD 相同；`git diff --quiet HEAD --` 检查通过。
- **本机跟踪引用异常：** 多次 fetch 显示快进，但随后读取本机 `origin/main` 仍为旧值，状态仍可能显示 ahead 104。此次一致性结论使用服务器直接查询 `git ls-remote origin refs/heads/main` 与本地 HEAD 的比较，不使用该过期缓存。没有为了清除提示而重写 main 历史。
- `artifacts/` 为原有未跟踪目录，没有上传或删除。测试后出现 `canvas-studio/`，报告位于 `outputs/`；这些不属于已同步的受版本控制代码。依赖目录、构建产物、项目工作记录也不属于服务器源码树。
- “代码一致”指受 Git 版本控制的内容与指定提交一致，不意味着整个磁盘目录逐字节相同，更不意味着安装包或已部署程序已经更新。
- 本轮没有访问真实用户数据库或密钥，没有调用真实模型服务，没有执行收费业务请求；未将本审查报告推送到公开仓库。

## 3. 架构、规模与覆盖范围

### 3.1 主要模块

| 层次 | 关键入口 | 职责与审查重点 |
| --- | --- | --- |
| 桌面主进程 | `src/main/index.ts`、`src/main/ipc/` | 窗口、媒体协议、IPC、项目与模型网关 |
| 受控桥接 | `src/preload/index.ts` | renderer 到主进程的接口与保存结果传播 |
| 画布与界面 | `src/renderer/src/canvas/`、`nodes/specs/` | tldraw 图快照、交互、节点正文与输出投影 |
| 执行内核 | `src/shared/engine/`、renderer/headless executor | 调度、输入收集、取消、循环、结果与状态 |
| 应用服务 | `src/application/`、`src/cli/`、`src/mcp/` | 无界面入口、工作流操作与项目存储适配 |
| 持久化 | `src/main/store/` | SQLite 索引、project.json、媒体、导入导出与恢复 |
| 工程门禁 | `test/`、CI、构建配置 | 契约、数据一致性、测试环境、构建可复现性 |

本项目按工程约束属于**本地单用户 Electron 软件**。未将没有登录、多租户权限或云同步判为缺陷。

### 3.2 规模口径

统计 `git ls-files src test` 中的 `.ts/.tsx/.css/.html`，按文件文本换行计数，包含注释与空行，排除依赖和 out/dist：

- 源码：237 个文件，约 59,169 行。
- 测试：78 个文件，约 13,680 行；其中 77 个测试套件文件，另有辅助文件。
- 这属于规模统计，**不是行覆盖率**，也不意味着每个文件均已人工逐行阅读。

| 大型模块 | 行数 | 建议拆分方向 |
| --- | --- | --- |
| `src/renderer/src/assets/app.css` | 9,684 | 设计变量、画布、节点、弹层分层，减少覆盖规则叠加 |
| `src/renderer/src/assets/ui-surfaces.css` | 2,131 | 与 app.css 明确职责，先做样式回归再迁移 |
| `src/renderer/src/canvas/CanvasEditor.tsx` | 1,845 | 保存协调、外部重载、菜单/拖放、画布生命周期 |
| `src/capabilities/definitions.ts` | 1,439 | 按节点类别组织，但保留统一注册与契约校验 |
| `src/renderer/src/canvas/DirectorStudioPanel.tsx` | 1,323 | 编辑状态、属性面板、发布操作分离 |
| `src/renderer/src/nodes/specs/bodies/video-transforms.tsx` | 1,135 | 按操作节点拆分，复用字段与状态呈现 |
| `src/renderer/src/canvas/graph.ts` | 1,020 | 图提取、连接规则、迁移和几何独立模块 |
| `src/renderer/src/engine/executor.ts` | 940 | 生命周期、调度和子流程边界分离 |

### 3.3 证据等级

- **源码确认：** 主审复核关键文件、条件分支及调用链，定位以本报告基线为准。
- **隔离验证：** 审查过程中对真实实现配合内存 Editor、存储、IPC、网关替身或假密文执行；证明局部逻辑，不等同真实桌面端到端。
- **自动化实测：** 本轮完整测试、类型检查、lint 和生产构建，原始结果随附件提供。
- **待验收：** 真实桌面关闭/重开、真实文件系统故障、安装包、真实供应商、网络边界、长时间压力与漏洞利用链完整性。

## 4. 问题总表

P1：可能造成数据丢失、越过承诺的安全边界或让关键流程静默产出错误，应优先修复。P2：条件性数据/行为偏差，应在相应能力全面验收前修复。未按本轮证据认定 P0。

| 编号 | 级别 | 问题 | 主要范围 |
| --- | --- | --- | --- |
| F01 | P1 | 保存早期失败后，回滚删除当前有效项目或恢复过旧版本 | 项目存储 |
| F02 | P1 | 关窗保存主动绕过版本检查，覆盖外部已提交修改 | 桌面与 CLI/MCP 并行使用 |
| F03 | P1 | 关窗保存失败结果被丢弃，窗口仍退出 | preload 与关闭生命周期 |
| F04 | P1 | headless 代码节点可越过 VM 上下文隔离 | 显式启用真实执行时 |
| F05 | P1 | headless 配置多包一层，桌面配置失效 | 无界面运行适配 |
| F06 | P1 | headless 通用投影丢失节点专属结果语义 | 结构化数据与媒体结果 |
| F07 | P1 | headless 保存 JSON 后，桌面快照正文被清空 | 跨端保存往返 |
| F08 | P1 | 循环中途取消后，部分完成项被当作 done 并在续跑跳过 | renderer 循环执行 |
| F09 | P2 | 持久化多上游输入被覆盖，只剩最后一路 | headless 单节点/选区运行 |
| F10 | P2 | 拆图单图端口忽略选中项，固定返回最后一个结果 | 节点输出投影 |
| F11 | P2 | 密钥暂时无法解密时，编辑名称会清除原密文 | 供应商配置 |

## 5. 详细发现与修复建议

### F01 · P1 · 保存早期失败会破坏原本有效的项目文件

**位置：** `src/main/store/projects.repo.ts:222–240`，特别是 225、234–237 行。

**根因：** 临时文件写入、旧文件轮转、新文件转正和数据库更新共用一个 catch。回滚不知道已经完成哪一步，始终把现有主文件移到 tmp，再恢复 bak，最后删除 tmp。

**触发：** 主文件为有效 v2，在写 `.tmp` 时发生磁盘满等错误。此时旧主文件尚未轮转。回滚却把 v2 移走并删除：如果有 bak，只剩旧 v1；如果没有 bak，则唯一有效主文件消失。新建或导入后尚无备份的项目尤其受影响。

**证据：** 关键源码已复核；隔离文件系统中注入 ENOSPC，观察到有备份时 v2 退回 v1、数据库仍为 v2，无备份时主文件消失。未对用户真实项目制造磁盘故障。

**修复：** 保存事务引入明确阶段标记；旧文件未成功轮转前，失败只清理本次 tmp；只有旧文件确已转为 bak 时才恢复 bak。回滚失败保留全部可恢复副本，不能继续清理唯一有效副本。后续评估 fsync 与崩溃一致性。

**回归验收：** 在序列化、tmp 写入、旧文件轮转、tmp 转正、SQLite 更新每一阶段注入异常，分别覆盖有/无 bak。断言最近一次成功保存的内容仍可读取、版本不错误回退。现有 `test/agent/graph-write-transaction.test.ts` 的数据库失败测试没有覆盖写 tmp 失败。

### F02 · P1 · 关闭窗口时可覆盖已经提交的外部更改

**位置：** `src/renderer/src/canvas/CanvasEditor.tsx:536–545`；`src/main/ipc/project.ipc.ts:124–133`；`src/main/store/projects.repo.ts:204–208`。

**根因：** 普通保存使用 expectedGraphVersion；关窗调用却显式传 `expectedGraphVersion: undefined`，变成最后写入者覆盖。

**触发：** 桌面仍显示版本 N，CLI/MCP 已保存 N+1，桌面尚未完成外部通知重载时关闭窗口。旧画布随后覆盖外部节点、连线或结果，并推进为 N+2。即使桌面没有未保存修改，当前关闭逻辑仍可能写整份图。

**证据：** 版本检查及关闭调用已复核。隔离对照中，同一旧载荷携带版本号会被拒绝，删除版本号后成功覆盖。未执行真实 Electron 关窗竞态。

**修复：** 关窗不应降低保存一致性标准。保留版本校验；冲突时阻止关闭并提供合并/恢复副本方案。没有 dirty 状态时不重复保存整份图；对本地未保存更改保留可恢复数据。

**回归验收：** 外部提交后、通知重载前关闭窗口；外部修改不能消失，本地未保存内容也必须有恢复路径。添加双入口竞态集成测试。

### F03 · P1 · 同步保存失败结果丢失，关闭仍继续

**位置：** `src/preload/index.ts:65–67`；`src/main/ipc/project.ipc.ts:126–132`；`src/renderer/src/canvas/CanvasEditor.tsx:539–545`。

**根因：** 主进程返回 FLUSH_FAILED，但 preload 将 sendSync 返回值丢弃，接口类型是 void；beforeunload 没有成功检查和阻止关闭逻辑。

**触发：** 用户仍有防抖队列中的修改，关闭时碰到写锁冲突或磁盘错误。保存失败，页面销毁，修改丢失。该问题独立于 F01，即使回滚完全正确，写锁占用也足以触发。

**证据：** 隔离 IPC 返回失败信封时，实际 preload 方法仍返回 undefined；关闭路径静态确认不会检查失败。

**修复：** 将同步 API 改为返回保存结果；更稳妥的是主进程协调关闭确认，保存成功才退出，失败保留窗口和 dirty 状态。项目不存在等空结果也不能当作保存成功。

**回归验收：** 写锁、I/O 错误、项目不存在分别触发关闭；确认错误可见、窗口保留、内容不丢失，成功重试后才允许退出。避免只测试 repo 而遗漏 preload 到界面的结果传播。

### F04 · P1 · headless VM 不是可靠的不可信代码安全边界

**位置：** `src/main/headless/run-code.ts:119–128`，尤其宿主 args 注入点 122 行。入口包括 `src/cli/index.ts:46`、`src/mcp/server.ts:666` 附近的 headless 执行装配。

**前提：** 必须显式开启 `CANVAS_AGENT_EXECUTE=enabled`，再执行代码节点；不是默认只读模式下任意文件一打开就可触发。

**根因：** 宿主创建的 args 对象直接放入 VM。对象的跨上下文构造器链可到达宿主动态函数能力；仅把上下文内 process、require、Function 赋值为禁用值，不能构成完整权限隔离。

**证据与边界：** 审查隔离验证仅设置一个无害宿主字符串标记，实际代码节点能够读出该标记。没有读取系统秘密、访问网络或修改文件。该结果确认越过上下文边界；不将其描述为已完成真实系统破坏验证。

**影响：** 对用户主动编写且可信的脚本，这是权限模型问题；对外部工作流或自动生成且尚未可信审核的脚本，则违反“离线数据转换、禁用网络/模块访问”的承诺，需要优先修复。

**修复：** 不依赖 node:vm 加关键词黑名单执行不可信代码。采用可限制文件与网络能力的隔离运行时，或具有操作系统权限约束的独立执行进程；输入输出走序列化边界，设置资源限额与可终止机制。普通子进程如果仍拥有同样权限，也不等于安全隔离。

**回归验收：** 宿主假标记不可读；输入对象及嵌套对象不能提供跨边界能力；网络/文件/模块能力受实际权限约束；超时后执行真正终止。`test/headless-run-code.test.ts` 已有直接 fetch/require/new Function 测试，但没有覆盖跨上下文对象路径。

### F05 · P1 · headless 错误解释桌面节点配置

**位置：** `src/main/headless/run-executor.ts:186–208`，198 行；对照 `src/shared/graph-snapshot-sync.ts:148` 与 `src/shared/engine/executors/processor.ts:52`。

**根因：** 桌面协议将真实配置字符串放在 `node.params.config`；headless 却使用 `JSON.stringify(node.params)` 作为 props.config，多包一层。

**触发样例：** 桌面 processor 的 params.config 为 `{"fallback":"固定值","valueType":"string"}`。headless 传入的是含 config 字符串属性的外层对象，executor 找不到顶层 fallback。

**证据：** 相同业务配置直接交给 shared executor 返回 done，经真实 headless 适配返回 skipped，原因为“没有输入变量或固定值”；整体 Run 仍可能显示 succeeded。

**修复：** 建立唯一 Node-to-Shape 适配器，明确从 params.config 还原配置。确需兼容其他输入格式时，在边界显式规范化并标注版本，不能各入口自行猜测。

**回归验收：** 同一个桌面保存 fixture 分别由 renderer/headless 执行，比较状态和完整结果。覆盖 processor fallback、路径选择、结构化 Schema，以及模型选项的读取，不能只测试 params 为空的文本节点。

### F06 · P1 · headless 输出投影与节点协议分离

**位置：** `src/main/headless/run-executor.ts:96–111,237–282`；`src/shared/engine/values.ts:142` 起；对照 `src/renderer/src/nodes/specs/outputProjections.ts:77–108,174–208`。

**根因：** headless 将 nodeResult 仅写到临时 shape，然后使用通用 NodeValue 解析及按端口类型回退。它没有复用节点注册的 projectOutputs，也没有完整保存节点结果、运行来源及多端口输出语义。

**场景一：** text A 输入 structured 模板 `{"message":"{{text}}"}`。executor 产生 structured-result，其 data 正确为 A；通用解析无法识别此封装，转而从正文投影，输出仍带模板占位符。

**场景二：** 图片裁剪网关替身返回新媒体。真实 executor 已写入 media-source，但通用解析无法识别，输出为空，Artifact 数为 0，保存图没有新媒体结果引用；Run 却可以显示成功。

**证据：** 已复核实际适配与解析实现，上述两条路径完成隔离验证。确认的是结果引用、输出和运行关联丢失，不是证明媒体服务一定没有生成文件。

**修复：** 将纯数据输出投影抽到 shared，与节点协议共同注册，renderer/headless 都使用；保存用户正文、运行记录和结果时保持边界；成功节点必须完成输出契约校验，多端口/集合产物均可追溯。

**回归验收：** structured 插值后接下游、mock crop 后接下游与 Artifact、保存重开、多输出节点均验证具体值。现有 renderer 投影测试通过不能证明 headless 采用相同语义。

### F07 · P1 · JSON 输出保存后清空桌面正文

**位置：** `src/main/headless/run-executor.ts:274–278`；`src/shared/graph-snapshot-sync.ts:146–164`；`src/application/stores/desktop-store.ts:86` 附近。

**根因：** headless 将 JSON 输出保存为 content.kind=json；快照同步器只把 text content 转成 props.text，其他情况统一写空字符串。

**触发：** 桌面 JSON 节点正文合法，经 headless 执行和生产保存适配后，其 graph content 仍有 JSON 数据，但 tldraw 节点 props.text 为空。重开桌面后正文显示和复跑语义被破坏。

**证据：** 真实 headless 与快照同步函数的隔离组合验证观察到该不一致；生产 DesktopProjectStore 保存入口已静态核对。未声称 SQLite 内 JSON 数据整体被删除。

**修复：** 明确 CanvasNode.content 与桌面正文的双向转换；JSON 内容应采用一致序列化。不要为补正文而把 structured 的用户模板直接换成运行产物，应区分编辑内容和结果。

**回归验收：** JSON 新建/更新/运行/保存/重开全流程，含已有快照、无快照、不同缩进；断言 graph 与 snapshot 表达一致且再次运行得到相同数据。

### F08 · P1 · 循环取消后的部分结果会被续跑复用

**位置：** `src/renderer/src/engine/executor.ts:701–735`；`src/shared/engine/executors/iterate.ts:228–263,338–345`。

**根因：** 子流程取消后 break 并返回已经完成节点的部分输出；runItem 在 await 返回后不重新检查取消，只要输出非空就将列表项记为 done。resume 随后复用 done/reused。

**触发：** 带稳定 id 的列表进入 iterate，循环体 A 已成功，B 执行期间停止。循环本身显示 cancelled，但该项被记 done；同列表续跑后该项变 reused，B 没有完成却不再运行。

**证据：** 使用真实 renderer 编排、契约投影与 shared executor，配合内存 Editor/store 和受控代码执行替身：正常对照包含 A/B，停止时只含 A，续跑 B 调用次数不增加。

**修复：** 子流程以显式 cancelled 结果或异常传播；runItem 在异步返回后再次检查 token。只有完整子流程完成才能进入可复用状态，部分产物可以留作诊断但不能代表完成。

**回归验收：** 多节点循环体在每个等待点取消→恢复；已完整完成项复用，未完成项重新执行。稳定 id/指纹检查仍需保留，但不能替代完成状态正确性。

### F09 · P2 · 持久化多上游输入静默截断

**位置：** `src/main/headless/run-executor.ts:211–234`，对比 221 行追加和 232 行覆盖。

**根因：** 本轮 packet 分支追加到端口数组；持久化回退分支却直接 map.set 为仅包含当前值的新数组。

**触发：** A、B 两个文本源连接到支持 many 的 T.in-text，仅运行正文为空的 T。A/B 都属于范围外持久化上游，T 最终只收到 B，不是协议约定的两段合并文本。

**证据：** 该双上游路径隔离复现。混合本轮与持久化来源也存在覆盖/顺序问题，应一并修复和验证。

**修复：** 逐条按稳定边顺序决定取本轮或持久化输出，再追加；不能简单在第二轮末尾追加而改变混合来源顺序。

**回归验收：** 两个范围外上游、两个本轮上游、混合来源、重复和缺失端口分别断言值、个数与顺序。扩展 `test/agent/headless-runner.test.ts` 的单上游场景。

### F10 · P2 · 拆图单图输出不是选中项

**位置：** `src/shared/engine/executors/imageSplit.ts:34`；`src/renderer/src/nodes/specs/outputProjections.ts:54,86–108`；协议见 `NODE_CONTRACT_SPEC.md:179` 附近。

**根因：** 拆图记录默认 selectedMediaId 为第一张，单图投影却调用固定取 results.at(-1) 的“最新结果”函数。

**触发与证据：** 2×2 拆分的网关替身返回四个媒体引用。实际记录选中 grid-1，out-image 却指向 grid-4；out-images 列表包含全部四项，所以仅测列表长度不会发现。

**修复：** 为拆图定义按 selectedMediaId 选择的投影；明确无效 ID 的回退行为。不要无差别改其他操作节点的“最新结果”语义。

**回归验收：** 默认选择、切换选择、无效 ID、空集合断言完整媒体引用。`test/projectNodeOutputs.test.ts` 已有多结果 fixture，但需补 mediaId 精确断言，不能只断言 kind。

### F11 · P2 · 解密暂不可用时，普通编辑清除原密文

**位置：** `src/main/gateway/providers.repo.ts:94–112`；`src/main/gateway/keycrypto.ts` 的解密失败空串返回路径。

**根因：** 留空密钥本应保留原值，但 saveProvider 先通过 getProvider 解密旧密钥，然后重新加密保存。解密失败返回空串后，保存分支把 api_key_ref 变成 null。

**触发：** 已有 enc: 密文，系统密钥服务暂不可用或解密异常。用户只改供应商名称/模型，按界面约定密钥留空，原密文被清除；系统恢复后也无法再解密已丢失的数据。

**证据：** 假密文、模拟不可用 safeStorage 与实际保存实现的隔离验证中，仅改名称即可使密文字段变 null。没有读取用户真实密钥。

**修复：** 未提交新密钥时直接保留数据库中的原始密文字段，不解密重加密。分别表达未配置、不可解密、主动清除三种状态；同步清理文件顶部关于明文存储的过时注释。

**回归验收：** 正常、解密不可用、解密抛错三种条件下，留空编辑均应逐字节保留密文；明确新密钥才替换，明确清除动作才删除。

## 6. 实测结果与测试环境问题

### 6.1 类型、规范与构建

- 托管 Node：22.22.2。使用现有项目依赖，没有重新安装或覆盖原生模块。
- `tsc --noEmit -p tsconfig.node.json --composite false` 与 web 配置分别通过，退出码 0。
- `eslint --no-cache .` 完整执行，退出码 0，但有 129 条格式告警。没有使用 --fix 改业务文件。
- 初次构建因内存分配失败中断；不再并行运行检查后，单独生产构建退出码 0。主进程、preload、renderer 均生成成功。
- renderer 构建处理 2,385 个模块，主要 JS 文件 8,833.63 kB，CSS 353.24 kB，均为构建日志中的未压缩体积。
- 未执行 electron-builder 安装包生成、签名、安装或升级；构建成功不等于包可用，也不证明原生依赖运行时匹配。

### 6.2 完整测试结果

默认并发第一次出现 V8 内存不足及 worker channel closed。低并发、禁用文件并行后完成全量测试：**77 文件，950 项，932 通过、18 失败、0 pending；71 文件通过、6 文件含失败。**

| 失败文件 | 失败数 | 当前阻断点 |
| --- | --- | --- |
| `test/project-persistence.test.ts` | 1 | SQLite 原生模块加载失败 |
| `test/providers-security.test.ts` | 1 | 同上 |
| `test/transfer.integration.test.ts` | 2 | 同上 |
| `test/video-transform-main.test.ts` | 3 | 同上；其中两项预期业务异常被模块异常替代 |
| `test/agent/concurrent-write-lock.test.ts` | 8 | SQLite 原生模块加载失败 |
| `test/agent/graph-write-transaction.test.ts` | 3 | SQLite 原生模块加载失败 |

本机 `better-sqlite3@12.11.1` 二进制为 **NODE_MODULE_VERSION 140**，本轮 Node 需要 **127**。这证明当前依赖目录与测试运行时不兼容，不证明上述 18 个业务功能各自有逻辑缺陷。必须在匹配运行时/隔离依赖环境中重跑，才可进一步判定。

**建议：** 将 Node 单测与 Electron 原生依赖构建分开维护，CI 明确每个任务的运行时与二进制 ABI，启动前输出 process.versions.modules 并做内存数据库加载冒烟；缓存键至少含操作系统、架构、锁文件、运行时和 ABI。不要为了跑 Node 测试直接覆盖桌面正在使用的 Electron 二进制。

本轮未重新编译该依赖，以避免影响现有桌面环境；这项验证缺口仍未关闭。虽然生产构建通过，完整 verify 仍不能认定通过。

### 6.3 覆盖率与 CI 的结构性空白

- `vitest.config.ts:28–39` 的 coverage.include 只列 8 个文件，不覆盖绝大多数主进程存储、headless、IPC 与跨端适配；且没有看到覆盖率阈值配置。本轮没有运行 coverage，不提供估算覆盖率百分比。
- `.github/workflows/ci.yml` 依次执行安装、类型检查、lint、测试、契约快照、生产构建；package.json postinstall 会执行 Electron 原生依赖安装步骤，而测试通过 Node 执行，应核对两阶段 ABI 切换。本轮未查询服务器 CI 状态，不断言远端 CI 成功或失败。
- 当前 lint 门禁允许告警通过；建议先清理本次 129 条格式告警，再引入稳定的零告警或“不新增告警”策略，避免一次性格式化掩盖修复差异。
- 现有测试对契约、迁移、单函数输入输出覆盖较好，但“桌面保存→headless 运行→快照同步→桌面重开”的组合测试不足，正是 F05–F07 能共存的原因。

## 7. 优化建议与分阶段落地

### 阶段 A：先保住数据与执行边界

1. 修复 F01，使用阶段化保存事务与故障注入矩阵；任何失败不能删除最后有效副本。
2. F02/F03 合并设计为 SaveCoordinator：dirty 标记、版本、单写队列、明确保存结果、冲突恢复副本、关闭确认。
3. F04 修复前，不将外部或自动生成代码当作可信脚本执行；保持真实执行默认关闭，明确授权范围。
4. F11 使用密文原样保留策略，避免把不可用误认为未配置。

**验收门槛：** 所有保存故障回归通过；外部修改/关窗竞态不丢数据；不可信脚本无法越过实际权限边界；没有真实用户数据参与故障试验。

### 阶段 B：让两个执行入口共享语义

1. 建立共享纯数据 NodeAdapter，统一 params.config、用户正文、媒体引用及运行结果转换，修复 F05/F07。
2. 共享节点 OutputProjector 与契约验证，移除 headless 的按端口类型猜测，修复 F06/F10。
3. 输入收集以稳定 edge 顺序单遍处理，修复 F09。
4. 将取消作为一等状态传播，区分未执行、取消、部分产物、完整成功，修复 F08。
5. 对所有可执行节点建立 renderer/headless fixture 对照；初期先覆盖不需真实网络的 text、JSON、processor、structured、code 与 mock 媒体操作。

**验收门槛：** 同一项目两入口得到相同的端口值、状态和可恢复结果；保存重开后结果与正文边界不变；部分执行不被标记为完整成功。

### 阶段 C：工程与性能优化

| 方向 | 具体建议 | 验收指标或证据 |
| --- | --- | --- |
| 原生依赖环境 | 分离 Node/Electron ABI，冻结锁文件，独立缓存 | 干净环境测试 950 项重跑，不再出现模块版本错误 |
| 测试资源 | 将契约、存储和 UI 分组，给并发设置资源预算 | 低资源环境无 OOM，失败有完整 JSON 报告 |
| 覆盖率 | 纳入 store、headless、shared/engine、preload；分阶段提高阈值 | 关键失败分支有测试，不仅追求总体百分比 |
| 调度 | `run-executor.ts:338–339` 的 queue.shift 改游标；优先复用共享拓扑实现 | 大型 DAG 基准对比，保持拓扑稳定与 O(V+E) 目标 |
| 图快照同步 | `graph-snapshot-sync.ts:132–139` 每次找最大 sibling index 扫全 store，可在批次内复用 | 大批量创建时记录耗时/内存，不预先宣称已有卡顿 |
| 首屏体积 | 对确实非首屏必需的重型面板和依赖做懒加载，观察打包依赖图 | 对照当前 8.83 MB 主 JS，记录冷启动和首次打开面板时间 |
| 组件结构 | 先从 CanvasEditor 提取保存协调与重载，不重写全部画布 | 生命周期回归无重复订阅、无保存语义退化 |
| 样式治理 | app.css / ui-surfaces / ui-foundation 明确分工 | 节点空态、输入态、结果态、缩放态截图回归 |
| 导入导出 | 在现有暂存与重映射上补结构/引用完整性检查，评估流式处理 | 损坏包、重复媒体、缺失引用、恢复中断测试 |
| 桌面加固 | 导航限制、IPC sender 校验、评估 sandbox 可用性 | 以可达攻击面和兼容测试验收，不把配置单独算漏洞 |

以上性能项为基于结构与构建体积的优化方向，本轮没有给出交互 FPS、冷启动时延或内存泄漏的实测结论。

### 推荐提交拆分

- 提交 1：保存事务回滚 + 各阶段故障测试（F01）。
- 提交 2：关闭保存协调 + 外部版本冲突 + 失败传播（F02/F03）。
- 提交 3：受限代码执行环境 + 边界回归（F04）。
- 提交 4：共享配置/正文适配 + JSON 往返（F05/F07）。
- 提交 5：共享输出投影、Artifact 与多端口持久化（F06/F10）。
- 提交 6：输入聚合与循环取消恢复（F08/F09）。
- 提交 7：密文保留（F11）、原生依赖测试隔离与 CI 增强。

每组提交应先写能重现现有问题的测试，再修复，再运行相关测试与全量门禁。若端口、Schema、configSchema 或契约版本改变，应按 AGENTS.md 同步 generated/agent-contracts.json 并处理 contractVersion；不能因为修复适配器而无理由更改公共协议。

## 8. 已有优点、限制与最终判断

### 已有基础值得保留

- 工程已经分出 shared engine、应用服务、renderer/headless 适配层，适合进一步统一语义，而不是整体推倒重写。
- 普通保存已有版本检查、排他写锁、备份、数据库与文件回滚思路，需补失败阶段正确性，而非重新引入另一套独立保存机制。
- 供应商列表使用公开摘要，不回传解密 Key；问题 F11 是编辑时丢密文，不是列表已经泄露密钥。
- 导入已有版本/条目数/声明解压体积检查、媒体文件名校验、引用递归重映射、暂存目录与事务；没有把已处理的路径问题重复列成漏洞。
- 已有 950 项自动化用例及契约快照/版本门禁。下一步重点应是边界组合和真实环境验证，不是单纯增加测试数量。

### 未完成的验证

- 18 项受 ABI 阻断的测试未通过；没有证明修复 ABI 后必然全部通过。
- 未完成真实 Electron 的关窗、外部并发编辑、跨端保存重开端到端验证。
- 未执行真实模型服务、收费调用、恶意媒体完整利用链、真实数据库故障、安装包升级/签名或长时间性能压力测试。
- 未运行依赖漏洞数据库扫描，因此不提供“依赖无已知漏洞”的结论。
- 未验证远端 CI 或运行中的部署；本轮只更新 GitHub 源码分支。
- 本机 origin/main 跟踪缓存读取异常仍保留；服务器直接查询的提交与本地一致。

**最终判断：** 项目具备清晰的工程基础，但保存与无界面执行边界仍有高优先级风险。本轮交付是“已同步源码 + 有证据的审查与修复计划”，不是“问题已修复”。建议先完成阶段 A/B，再在匹配 ABI 的环境中复跑全部测试与桌面回归，之后才决定是否发布。

## 附录：验证材料

- `tests-serial-final.json`：完整 950 项测试结果与失败堆栈。
- `tests-serial-final.log`：最终低并发运行日志。
- `tests.log`：默认并发首次运行日志，含 ABI 错误与 OOM 记录。
- `lint.log`：129 条格式告警原文。
- `build.log`：初次构建内存分配失败记录。
- `build-retry.log`：成功生产构建记录与产物体积。
- `验证摘要.json`：机器可读汇总及同步核验信息。

本报告所有源码位置均相对于基线提交 555a77ec0d738f5d35c7b1a4d922391f61b93231。后续代码变化可能使行号漂移。
