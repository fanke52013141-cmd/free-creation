# 无限画布 · 开发计划与路线图

> **配套文档**：本文件是执行路线图，权威规范见 [`开发规范-铁律v1.0.md`](./开发规范-铁律v1.0.md)（架构铁律），技术调研见 [`无限画布-开源方案调研报告.md`](./无限画布-开源方案调研报告.md)。
> **代码位置**：前端在 [`app/`](./app/)（React 19 + TypeScript + Vite 8 + tldraw 5.3.2 + Tailwind v4）。
> **最近更新**：本轮（节点面板重构为左侧竖排 + tooltip + 拖拽创建）。

---

## 一、当前状态（MVP-0 已完成）

### 1.1 已交付能力

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 课程/章节/项目三级树 | ✅ 完成 | CRUD + 拖拽改变层级归属（项目拖入章节/课程、章节拖出课程），遵循铁律§2.2 |
| 无限画布 | ✅ 完成 | 集成 tldraw，每项目独立 `persistenceKey` 自动持久化 |
| 聊天节点 ChatNode | ✅ 完成 | 自定义 shape：系统提示词/模型选择/温度/最大长度；对话沉淀；真实调用 LLM（OpenAI 兼容） |
| 文本资产节点 TextAsset | ✅ 完成 | 文本载体，双击编辑，可被聊天节点引用为上下文（数据依赖） |
| 节点间数据引用 | ✅ 完成 | 聊天节点⚙面板下拉选文本资产；被引用文本资产高亮"●被引用" |
| Model 配置面板 | ✅ 完成 | 模型列表 CRUD（provider/baseUrl/modelId/key/温度/最大长度/默认） |
| localStorage 持久化 | ✅ 完成 | 树结构 + Model 配置；画布由 tldraw 引擎自动持久化 |
| 左侧节点面板 | ✅ 完成 | 竖排分类图标 + hover 详细 tooltip + 拖拽到画布创建 |

### 1.2 当前技术栈（已落地）

- **前端**：React 19.2.8 + TypeScript 6 + Vite 8.2.1
- **画布引擎**：tldraw 5.3.2（自定义 shape 走 `BaseBoxShapeUtil` + module augmentation 注册类型）
- **样式**：Tailwind CSS v4（`@import "tailwindcss"` + `@tailwindcss/vite` 插件）
- **状态层**：自研轻量 store（`useSyncExternalStore` + localStorage）
- **包管理**：pnpm 11.7.0，Node 24

### 1.3 已知问题与技术债

| 编号 | 问题 | 严重度 | 计划处理阶段 |
| --- | --- | --- | --- |
| D-1 | LLM 调用走浏览器直连，生产环境会遇 CORS；需后端代理 | 高 | 第二阶段（后端） |
| D-2 | 画布快照存浏览器 IndexedDB（tldraw 默认），换浏览器丢失；未持久化到项目级后端 | 中 | 第二阶段 |
| D-3 | 未实现的节点类型在面板里是灰色占位，点击/拖拽无反应 | 低 | 按路线图逐步实现 |
| D-4 | 无自动化 UI 测试，交互正确性依赖手动验证 | 中 | 第二阶段引入 Playwright |
| D-5 | 富文本编辑未接入 TipTap（规范§6.3 建议） | 低 | 第三阶段 |

---

## 二、整体路线图（阶段划分）

路线图以《规范》§6.4 的 MVP 路线图为基准，按"先跑通创作闭环、再补处理能力、最后做工作流封装"的节奏推进。每个阶段都有**可演示的验收标准**，不追求单阶段完美。

```
MVP-0  基础骨架 + 聊天/文本节点          ✅ 已完成
MVP-1  单次处理节点 + Split/Merge        ← 下一阶段
MVP-2  图片生成节点（中转站 Image 2）
MVP-3  后端落地（FastAPI + 模型代理 + 资产存储）
MVP-4  视频生成节点（MiniMax H3 / Seedance 2.0）
MVP-5  处理类 op（图像/视频/音频）
MVP-6  框选运行 + 工作流封装/解封装（双态闭环）
MVP-7  标注修图（指哪打哪）+ 资产节点完善
MVP-8  打磨与发布
```

---

## 三、各阶段详细计划

### MVP-1：单次处理节点 + Split/Merge（下一阶段）

**目标**：跑通"剧本 → 分镜拆分 → 批量引用"的最小批量生产闭环。

**交付项**：
1. **单次处理节点 OneShotNode**（自定义 shape）
   - 形态：输入区 + 一键执行 + 结构化输出区
   - 配置：prompt 模板（含 `{{变量}}` 插槽）、模型、温度、输出 schema
   - 铁律§4.5：分镜生成/资产提取/文案润色/翻译都用这**一个**节点换 prompt，不为每种做节点
2. **Split 节点**：输入 Text，按分隔符（默认 `\n`/`===`/`---`，可自定义）拆成 `Text[]`
3. **Merge 节点**：`Text[]` 合并回 Text（先做文本合并，Image/Video 拼接后续）
4. **数组扇出**（铁律§3.4）：下游节点输入是数组时自动对每个元素各跑一次
   - 改造节点运行逻辑：单值跑一次、数组自动 map
5. **节点间引用关系可视化**：画布上画出数据依赖连线（不只是下拉选）

**验收标准**：能在画布上摆 `[文本资产·剧本] → [单次处理·分镜生成] → [Split] → [聊天节点 ×N 自动map]`，一键产出多套分镜文案并沉淀。

**关键技术点**：
- 数组扇出的实现：节点运行时检测输入类型，单值走原逻辑，数组则展开成子任务队列
- 引用连线：用 tldraw 的 arrow shape 或自定义绑定 shape 表达数据依赖

**预计周期**：1～1.5 个开发周

---

### MVP-2：图片生成节点（中转站 Image 2）

**目标**：跑通"prompt + 参考图 → AI 生成图片 → 在画布上呈现为可缩放媒体"。

**交付项**：
1. **图片生成节点 ImageGen**（异步任务节点）
   - 配置：prompt、参考图引用、生成参数
   - 异步任务状态机（铁律§5.3）：`pending → running → done`，失败可重试
   - 结果呈现为可缩放图片 shape
2. **图片资产节点 ImageAsset**：上传入库 + 预览
3. **任务队列最小版**（前端）：并发控制、进度展示、失败重试

**验收标准**：能在画布上摆 `[聊天节点·出 prompt] → [图片生成]`，运行后图片嵌入画布，可缩放、可基于结果再生成。

**关键技术点**：
- Image 2 接口适配（OpenAI 兼容格式可能不完全匹配，需适配器）
- 图片产物存储：MVP 阶段先存浏览器（blob URL 或 IndexedDB），后端落地后转对象存储

**预计周期**：1 个开发周

---

### MVP-3：后端落地（关键里程碑）

> **说明**：前两个 MVP 是纯前端，验证交互与画布体验。MVP-3 引入后端，解决 D-1（CORS）、D-2（持久化）、为视频生成等长任务铺路。**规范明确**：参考既有 PPT 项目的 FastAPI 架构。

**交付项**：
1. **FastAPI 后端骨架**：项目目录结构、路由、数据库（SQLite）
2. **模型中转代理**：所有 LLM/图片/视频调用经后端转发，解决 CORS + 隐藏 key
3. **资产存储**：项目独立文件目录（沿用 PPT 项目 `project_storage` 安全路径设计）
4. **画布快照持久化**：tldraw snapshot 存后端，跨设备同步
5. **异步任务队列**：图片/视频生成的长任务由后端调度（Celery 或轻量 asyncio 队列）
6. **前端对接**：把现有 localStorage 逻辑改为「本地缓存 + 后端同步」

**验收标准**：刷新换浏览器不丢数据；图片生成走后端无 CORS；视频生成任务在后端排队。

**关键技术点**：
- 数据模型按规范§2.3 落库（`canvas_snapshot`/`nodes_state`/`assets_ref`）
- 复用 PPT 项目的 `course_service.py` / `project_service.py` 的层级 CRUD 与拖拽语义
- 认证：MVP 阶段先单用户（无登录），后续加多用户

**预计周期**：1.5～2 个开发周

---

### MVP-4：视频生成节点（MiniMax H3 / Seedance 2.0）

**交付项**：
1. **视频生成节点 VideoGen**（异步长任务）
   - 进度轮询或 WebSocket 推送
   - 结果呈现为画布上的视频播放器 shape
2. **视频资产节点 VideoAsset**

**关键技术点**：长任务队列、轮询/推送、视频产物存储与缩略图生成。

**预计周期**：1 个开发周

---

### MVP-5：处理类 op（图像/视频/音频）

**交付项**：按规范§4.2 的 op 注册表实现：
- **图像处理**：split/crop/resize/format（非 AI）+ removeBg/toLineArt（AI）
- **视频处理**：trim/extractFrame/extractAudio/subclip（非 AI）+ toDepth/toWhiteMembrane（AI）
- **音频处理**：trim

**铁律提醒**：处理能力一律以 op 挂到处理节点，**不为每个 op 新建节点**。节点拓扑不变。

**关键技术点**：非 AI 的 op 可走 ffmpeg-wasm（前端）或后端 ffmpeg；AI 的 op 调模型。

**预计周期**：1.5 个开发周

---

### MVP-6：框选运行 + 工作流封装/解封装（双态闭环）

这是规范的核心创新点（双态模型），也是与扣子等传统工作流引擎的根本区别。

**交付项**：
1. **框选运行**（铁律§5.1）：选中多节点 → "运行所选"，按引用关系算拓扑序，无依赖并行
2. **工作流封装**：框选子图 + "封装为工作流" → 冻结成工作流单元，对外暴露输入/输出端口
3. **工作流解封装**：随时展开回发散态继续编辑
4. **工作流复用**：工作流单元可拖到别的画布当"子工作流节点"
5. **节点运行内核**：自研轻量调度器（节点 run + 依赖拓扑 + 输出缓存），参考 LangFlow step-by-step 与扣子 `WorkflowNodeDebugV2`

**验收标准**：能把"分镜批量生产"的 6 节点流水线封装成工作流，一键运行产出 10 套图文视频。

**预计周期**：2 个开发周

---

### MVP-7：标注修图 + 资产节点完善

**交付项**：
1. **标注修图节点 AnnotateEdit**（铁律§4.5：画标注框指哪打哪）
2. **音频/文档资产节点**完善
3. **文本预览节点 TextPreview**（呈现绑定，铁律§3.2）

**预计周期**：1 个开发周

---

### MVP-8：打磨与发布

- 富文本接入 TipTap
- 自动化测试（Playwright）覆盖核心交互
- 性能优化（大画布、多节点的渲染与运行）
- 错误处理与用户引导完善
- 打包发布

**预计周期**：1.5 个开发周

---

## 四、技术路线

### 4.1 前端

| 层 | 选型 | 说明 |
| --- | --- | --- |
| 框架 | React 19 + TypeScript | 已落地 |
| 构建 | Vite 8 | 已落地 |
| 画布引擎 | tldraw 5.3.2 | 已落地；自定义 shape 走 BaseBoxShapeUtil + module augmentation |
| 样式 | Tailwind CSS v4 | 已落地 |
| 状态 | 自研 store（useSyncExternalStore） | MVP 够用；节点变多后评估引入 zustand |
| 富文本 | TipTap | 第三阶段接入（规范§6.3） |
| schema 校验 | zod | 后端对接时引入 |

### 4.2 节点运行内核（自研）

不引入重型工作流引擎，自研轻量调度器：

```
NodeRunner {
  run(node, inputs):  // 单节点运行
    解析输入优先级（显式引用 > 缓存 > 手填）  // 铁律§5.4
    检测输入是否数组 → 自动 map 扇出          // 铁律§3.4
    调用节点的 execute()
    缓存输出
  runSelection(nodes):  // 框选运行
    按数据依赖算拓扑序
    无依赖节点并行
    临时跑一遍（不改变画布固化状态）
  runWorkflow(unit):  // 工作流运行
    按封装时冻结的顺序执行
}
```

参考 LangFlow 的 step-by-step 与扣子 `WorkflowNodeDebugV2` 的单节点调试机制。

### 4.3 后端

- **框架**：FastAPI（沿用既有 PPT 项目架构）
- **数据库**：SQLite（沿用）
- **文件存储**：项目独立目录 + `project_storage` 安全路径（沿用 PPT 项目）
- **任务队列**：图片/视频生成的长任务，MVP 用 asyncio 轻量队列，规模化后上 Celery
- **模型代理**：所有 AI 调用经后端转发，前端不持有 key

### 4.4 模型接入（铁律§2.5 首轮接入）

| 用途 | 模型 | 接入方式 |
| --- | --- | --- |
| 聊天/单次处理 | 中转站对话模型 | OpenAI 兼容 /chat/completions |
| 图片生成 | 中转站 Image 2（ChatGPT Image） | 适配器 |
| 视频生成 | MiniMax H3 + Seedance 2.0 | 适配器 |
| ComfyUI | 暂不接入（铁律） | — |

### 4.5 画布引擎备选方案

若 tldraw 商业许可成本不可接受（规范§6.1），退回 **React Flow**（MIT）。当前优先 tldraw，因其多媒体自由呈现是本产品核心。

---

## 五、节点实现进度跟踪

| 节点 | 类型 | 阶段 | 状态 |
| --- | --- | --- | --- |
| 聊天节点 ChatNode | 生成 | MVP-0 | ✅ |
| 文本资产 TextAsset | 资产 | MVP-0 | ✅ |
| 单次处理 OneShotNode | 生成 | MVP-1 | ⬜ |
| Split 拆分 | 工具 | MVP-1 | ⬜ |
| Merge 合并 | 工具 | MVP-1 | ⬜ |
| 图片生成 ImageGen | 生成 | MVP-2 | ⬜ |
| 图片资产 ImageAsset | 资产 | MVP-2 | ⬜ |
| 视频生成 VideoGen | 生成 | MVP-4 | ⬜ |
| 视频资产 VideoAsset | 资产 | MVP-4 | ⬜ |
| 图像处理 ImageProcess | 处理 | MVP-5 | ⬜ |
| 视频处理 VideoProcess | 处理 | MVP-5 | ⬜ |
| 音频处理 AudioProcess | 处理 | MVP-5 | ⬜ |
| 音频资产 AudioAsset | 资产 | MVP-7 | ⬜ |
| 文档资产 DocAsset | 资产 | MVP-7 | ⬜ |
| 标注修图 AnnotateEdit | 生成 | MVP-7 | ⬜ |
| 文本预览 TextPreview | 工具 | MVP-7 | ⬜ |

---

## 六、节奏建议

- **单阶段聚焦**：每个 MVP 阶段只做该阶段清单内的事，不提前实现后续阶段的节点（避免范围蔓延）。
- **可演示优先**：每个阶段结束必须能在画布上跑通一个真实场景，而不是"代码写完但跑不起来"。
- **规范先行**：动任何节点/关系前，先对照《规范-铁律》归类（节点 vs op、三种关系、类型契约），违反即返工。
- **技术债显式记录**：发现的问题记到本文件 §1.3 的技术债表，按阶段处理，不积压。
