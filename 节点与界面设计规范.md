# 节点与界面设计规范

## 目标

本规范服务于单机、单用户的无限画布创作工具。画布中的连线只表示真实的数据输入；节点不会因为视觉上相邻或普通箭头而隐式读取其他节点。界面以「项目导航 → 画布 → 底部创作入口」为固定层级，避免将每项能力长期暴露在屏幕上。

LibTV 使用指南带来的产品参考仅限于创作流：先沉淀脚本与资产，再进行批量图像/视频生成；不复刻其品牌、标识或具体界面。

## 节点契约

每个节点必须在 `app/src/shapes/nodeDefinitions.ts` 声明，不能在组件内部自行约定输入输出。定义包含：

- `type`：稳定、唯一的机器标识，创建后不得改名。
- `name`、`desc`、`category`、`icon`、`color`：用于底部节点入口和可发现性。
- `defaultSize`：当前统一为 `360 × 360`；节点内部用滚动容纳更多内容，不能以功能复杂为由改变默认大小。
- `inputs` / `outputs`：端口名称和 `ValueKind`。只有这里允许定义数据类型。
- `implemented`：未实现节点只能展示为规划，不可创建、不可参与数据图。
- `resolveOutputKind`：仅在输出类型由运行结果决定时使用，例如单次处理的单条/数组输出。

### 当前值类型

`Text`、`Text[]`、`Prompt`、`Prompt[]`、`ChatHistory`、`Structured`、`Image`、`Image[]`、`Video`、`Video[]`、`Audio`、`Audio[]`、`Doc`。

节点之间只有在源输出类型属于目标端口允许类型时才能连接。数组与单值不自动互转：使用 `Split` 把文本变为 `Text[]`，使用 `Merge` 把 `Text[]` 变为 `Text`。这避免批量生成、批量视频与单条编辑互相误接。

### 连线规则

1. 选择目标节点的输入来源时，系统同时保存输入引用和一条锁定的数据箭头。
2. 箭头的元数据必须包含源、目标、目标端口与已验证的值类型；删除或替换输入时同步删除旧箭头。
3. 连接时检查类型与环路；不兼容或会回流的连接直接报错，不创建“看似连上”的线。
4. 上游内容、配置或资产改变后，只把下游标记为 `dirty`，绝不自动发起收费模型调用。
5. 图检查必须检查断链、引用/连线不一致、端口类型不兼容和环路。

## 已实现节点与职责

| 节点 | 输入 | 输出 | 责任边界 |
| --- | --- | --- | --- |
| 文本资产 | 无 | Text | 本地可编辑文本的唯一基础载体。 |
| 图片资产 | 无 | Image | 上传或公开 URL 入库后给出稳定本地引用。 |
| 聊天节点 | 可选 Text 上下文 | ChatHistory | 保存完整对话历史，不作为隐式全局上下文。 |
| 单次处理 | Text / Text[] / ChatHistory / Structured | Text 或 Text[] | 按输入逐项处理，数组输入显式扇出。 |
| Split | Text / ChatHistory / Structured | Text[] | 按明确规则拆分，供批处理使用。 |
| Merge | Text[] | Text | 按连接符合并数组，绝不接收单条文本。 |
| 图片生成 | Prompt 类，可选 Image 类 | Image[] | 后端任务、结果本地入库、可恢复查询。 |
| 视频生成 | Prompt 类，可选 Image 类 | Video[] | 后端队列、首帧/参考图、可恢复查询和取消状态。 |

音频、文档、视频资产及处理节点均已登记，但 `implemented: false`；在具备端口契约、运行器、结果持久化、状态显示和测试前，不得开放创建。

## 新节点准入清单

新增节点必须一次完成以下事项：

1. 在 NodeDefinition 中登记端口、统一尺寸、分类与实现状态。
2. 在 `types.ts` 定义严格 props；运行状态至少支持 `idle`、`dirty`、`queued`、`running`、`done`、`error`、`canceled` 中适用的状态。
3. 实现 `getXValue` 读写适配；不允许组件直接猜测另一个节点的 props。
4. 使用 `replaceDataDependency` 创建或替换输入；禁止只保存 sourceId 而不创建箭头。
5. 将上游变更传播为 dirty；生成节点必须显式点击运行，支持失败信息和可恢复任务 ID。
6. 外部结果必须保存到项目资产目录，不能只保存短期 URL 或 data URL。
7. 补充契约测试、运行器测试和至少一条错误路径测试，并通过图检查。
8. 采用共享 `node-card` 壳、`node-header`、`node-config`、`node-body`、`node-footer` 和状态组件；不添加新的默认节点尺寸或独立颜色体系。

## 界面标准

- 色彩：石墨深色工作台，紫色为主动作，蓝青色为数据流提示；同屏不以大量高饱和颜色竞争注意力。
- 项目侧栏：只放创作集、章节、项目和本地模型状态。
- 顶栏：只放项目上下文、图检查、本地快照和尺寸迁移。
- 画布：创作主体，保留 tldraw 导航与选择能力，不放永久的节点长菜单。
- 底部 Dock：唯一的常驻创建入口。点击或拖拽创建节点；节点详情仅在悬浮时出现。
- 节点：统一 `360 × 360`、12px 圆角、顶部细色条、标题/设置/主体/操作四段。节点颜色仅编码类别，不编码优先级。
- 图标资产：`app/public/brand/canvas-mark.svg` 是原创产品标记，`flow-line.svg` 是数据流辅助资产；节点图标使用统一的简洁符号，不使用大面积 emoji。

## 本地运行边界

后端只服务本机；模型凭据只从 `backend/.env` 读取，且该文件不进入 Git。没有多租户、登录、权限校验或教程路径。项目资产、任务和画布快照保存在本机 backend 数据目录；生成任务重启后重新进入队列，但不会自动触发下游节点。
